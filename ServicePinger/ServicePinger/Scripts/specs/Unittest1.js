'use strict'
describe('app.controllers', function () {
   
    // Initialize the controller and a mock scope
    beforeEach(function() {
      
        });

    it('should ', function () {
        expect(0===0);
    });
});