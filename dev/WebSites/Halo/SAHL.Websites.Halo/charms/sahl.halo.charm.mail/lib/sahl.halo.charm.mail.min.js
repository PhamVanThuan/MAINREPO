'use strict';
angular.module('sahl.halo.charm.mail.app', [
    'sahl.js.core.applicationModules'
])
.service('$mailCharmService', ['$charmManagerService',
function ($charmManagerService) {
    return {
        start: function () {
            $charmManagerService.registerCharm('user', 'mail', 'sahl.halo.charm.mail', 'mail.png', 'click', 'mail.tpl.html');
        }
    };
}])
.controller('MailCharmCtrl', ['$scope', function MailController($scope) {
}]);