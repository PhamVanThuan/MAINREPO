using Machine.Specifications;

namespace WorkflowMaps.LoanAdjustments.Specs.Activities.No_Longer_Required.OnGetStageTransition
{
    [Subject("Activity => No_Longer_Required => OnGetStageTransition")] // AutoGenerated
    internal class when_no_longer_required : WorkflowSpecLoanAdjustments
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_No_Longer_Required(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}