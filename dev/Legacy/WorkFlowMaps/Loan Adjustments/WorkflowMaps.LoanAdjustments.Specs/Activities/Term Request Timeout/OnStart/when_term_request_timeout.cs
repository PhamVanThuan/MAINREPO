using Machine.Specifications;

namespace WorkflowMaps.LoanAdjustments.Specs.Activities.Term_Request_Timeout.OnStart
{
    [Subject("Activity => Term_Request_Timeout => OnStart")] // AutoGenerated
    internal class when_term_request_timeout : WorkflowSpecLoanAdjustments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Term_Request_Timeout(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}