using Machine.Specifications;

namespace WorkflowMaps.LoanAdjustments.Specs.Activities.Approve_Term_Change.OnGetStageTransition
{
    [Subject("Activity => Approve_Term_Change => OnGetStageTransition")] // AutoGenerated
    internal class when_approve_term_change : WorkflowSpecLoanAdjustments
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Approve_Term_Change(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}