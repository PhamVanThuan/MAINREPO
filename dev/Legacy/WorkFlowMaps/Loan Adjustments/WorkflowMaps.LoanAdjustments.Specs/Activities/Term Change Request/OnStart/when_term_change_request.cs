using Machine.Specifications;

namespace WorkflowMaps.LoanAdjustments.Specs.Activities.Term_Change_Request.OnStart
{
    [Subject("Activity => Term_Change_Request => OnStart")] // AutoGenerated
    internal class when_term_change_request : WorkflowSpecLoanAdjustments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Term_Change_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}