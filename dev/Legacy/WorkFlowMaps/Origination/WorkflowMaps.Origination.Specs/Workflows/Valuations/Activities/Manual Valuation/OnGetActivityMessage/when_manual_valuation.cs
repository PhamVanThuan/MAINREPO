using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Manual_Valuation.OnGetActivityMessage
{
    [Subject("Activity => Manual_Valuation => OnGetActivityMessage")] // AutoGenerated
    internal class when_manual_valuation : WorkflowSpecValuations
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Manual_Valuation(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}