using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Set_Active_Valuation.OnStart
{
    [Subject("Activity => Set_Active_Valuation => OnStart")] // AutoGenerated
    internal class when_set_active_valuation : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Set_Active_Valuation(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}