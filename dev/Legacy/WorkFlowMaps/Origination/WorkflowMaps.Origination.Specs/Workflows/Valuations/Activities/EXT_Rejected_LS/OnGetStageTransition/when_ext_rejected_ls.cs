using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.EXT_Rejected_LS.OnGetStageTransition
{
    [Subject("Activity => EXT_Rejected_LS => OnGetStageTransition")] // AutoGenerated
    internal class when_ext_rejected_ls : WorkflowSpecValuations
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_EXT_Rejected_LS(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}