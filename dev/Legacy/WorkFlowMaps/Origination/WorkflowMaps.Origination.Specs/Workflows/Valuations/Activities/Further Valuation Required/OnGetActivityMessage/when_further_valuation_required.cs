using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Further_Valuation_Required.OnGetActivityMessage
{
    [Subject("Activity => Further_Valuation_Required => OnGetActivityMessage")] // AutoGenerated
    internal class when_further_valuation_required : WorkflowSpecValuations
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Further_Valuation_Required(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}