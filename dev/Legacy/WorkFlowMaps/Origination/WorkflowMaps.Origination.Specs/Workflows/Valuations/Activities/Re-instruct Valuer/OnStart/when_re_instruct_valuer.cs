using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Re_instruct_Valuer.OnStart
{
    [Subject("Activity => Re_instruct_Valuer => OnStart")] // AutoGenerated
    internal class when_re_instruct_valuer : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Re_instruct_Valuer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}