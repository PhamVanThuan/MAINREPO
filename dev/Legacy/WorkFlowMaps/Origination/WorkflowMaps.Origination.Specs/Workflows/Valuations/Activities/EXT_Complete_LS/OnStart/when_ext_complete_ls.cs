using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.EXT_Complete_LS.OnStart
{
    [Subject("Activity => EXT_Complete_LS => OnStart")] // AutoGenerated
    internal class when_ext_complete_ls : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_EXT_Complete_LS(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}