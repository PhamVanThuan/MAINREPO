using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Note.OnGetStageTransition
{
    [Subject("Activity => Note => OnGetStageTransition")] // AutoGenerated
    internal class when_note : WorkflowSpecValuations
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_Note(messages, workflowData, instanceData, paramsData);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}