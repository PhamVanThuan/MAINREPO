using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.EXT_Complete_LS.OnGetStageTransition
{
    [Subject("Activity => EXT_Complete_LS => OnGetStageTransition")] // AutoGenerated
    internal class when_ext_complete_ls : WorkflowSpecValuations
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_EXT_Complete_LS(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}