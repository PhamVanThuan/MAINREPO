using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Request_Automated_Valuation.OnStart
{
    [Subject("Activity => Request_Automated_Valuation => OnStart")] // AutoGenerated
    internal class when_request_automated_valuation : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Request_Automated_Valuation(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}