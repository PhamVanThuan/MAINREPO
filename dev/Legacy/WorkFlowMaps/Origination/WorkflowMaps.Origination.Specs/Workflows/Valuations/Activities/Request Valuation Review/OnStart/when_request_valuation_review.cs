using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Request_Valuation_Review.OnStart
{
    [Subject("Activity => Request_Valuation_Review => OnStart")] // AutoGenerated
    internal class when_request_valuation_review : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Request_Valuation_Review(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}