using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Reassign_User.OnGetActivityMessage
{
    [Subject("Activity => Reassign_User => OnGetActivityMessage")] // AutoGenerated
    internal class when_reassign_user : WorkflowSpecValuations
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Reassign_User(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}