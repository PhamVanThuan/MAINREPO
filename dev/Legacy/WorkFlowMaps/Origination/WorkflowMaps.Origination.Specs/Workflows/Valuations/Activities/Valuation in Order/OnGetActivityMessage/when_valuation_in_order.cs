using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Valuation_in_Order.OnGetActivityMessage
{
    [Subject("Activity => Valuation_in_Order => OnGetActivityMessage")] // AutoGenerated
    internal class when_valuation_in_order : WorkflowSpecValuations
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Valuation_in_Order(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}