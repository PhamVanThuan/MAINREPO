using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Re_instruct_Valuer.OnGetActivityMessage
{
    [Subject("Activity => Re_instruct_Valuer => OnGetActivityMessage")] // AutoGenerated
    internal class when_re_instruct_valuer : WorkflowSpecValuations
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Re_instruct_Valuer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}