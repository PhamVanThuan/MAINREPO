using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Valuation_Request.OnStart
{
    [Subject("Activity => Valuation_Request => OnStart")] // AutoGenerated
    internal class when_valuation_request : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Valuation_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}