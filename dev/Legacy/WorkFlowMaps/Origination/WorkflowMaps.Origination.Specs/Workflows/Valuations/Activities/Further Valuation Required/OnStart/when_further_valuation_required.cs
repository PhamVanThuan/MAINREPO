using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Further_Valuation_Required.OnStart
{
    [Subject("Activity => Further_Valuation_Required => OnStart")] // AutoGenerated
    internal class when_further_valuation_required : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Further_Valuation_Required(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}