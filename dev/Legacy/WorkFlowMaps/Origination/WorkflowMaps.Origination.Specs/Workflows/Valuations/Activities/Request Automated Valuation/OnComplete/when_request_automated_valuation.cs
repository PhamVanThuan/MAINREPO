using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Request_Automated_Valuation.OnComplete
{
    [Subject("Activity => Request_Automated_Valuation => OnComplete")] // AutoGenerated
    internal class when_request_automated_valuation : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Request_Automated_Valuation(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}