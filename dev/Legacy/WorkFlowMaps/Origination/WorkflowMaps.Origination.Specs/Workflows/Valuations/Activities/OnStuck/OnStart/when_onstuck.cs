using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.OnStuck.OnStart
{
    [Subject("Activity => OnStuck => OnStart")] // AutoGenerated
    internal class when_onstuck : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_OnStuck(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}