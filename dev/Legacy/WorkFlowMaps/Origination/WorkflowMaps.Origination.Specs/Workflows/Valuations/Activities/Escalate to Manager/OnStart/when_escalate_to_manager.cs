using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Escalate_to_Manager.OnStart
{
    [Subject("Activity => Escalate_to_Manager => OnStart")] // AutoGenerated
    internal class when_escalate_to_manager : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Escalate_to_Manager(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}