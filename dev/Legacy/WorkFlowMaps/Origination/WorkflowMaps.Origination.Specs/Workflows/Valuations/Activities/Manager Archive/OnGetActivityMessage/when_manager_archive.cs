using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.Manager_Archive.OnGetActivityMessage
{
    [Subject("Activity => Manager_Archive => OnGetActivityMessage")] // AutoGenerated
    internal class when_manager_archive : WorkflowSpecValuations
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Manager_Archive(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}