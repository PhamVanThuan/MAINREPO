using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.Activities.EXT_CommonArchive.OnStart
{
    [Subject("Activity => EXT_CommonArchive => OnStart")] // AutoGenerated
    internal class when_ext_commonarchive : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_EXT_CommonArchive(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}