using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Common_Stuck.OnEnter
{
    [Subject("State => Common_Stuck => OnEnter")] // AutoGenerated
    internal class when_common_stuck : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Common_Stuck(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}