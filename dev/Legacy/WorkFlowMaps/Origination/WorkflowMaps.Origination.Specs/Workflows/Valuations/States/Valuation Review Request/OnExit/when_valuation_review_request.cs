using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Valuation_Review_Request.OnExit
{
    [Subject("State => Valuation_Review_Request => OnExit")] // AutoGenerated
    internal class when_valuation_review_request : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Valuation_Review_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}