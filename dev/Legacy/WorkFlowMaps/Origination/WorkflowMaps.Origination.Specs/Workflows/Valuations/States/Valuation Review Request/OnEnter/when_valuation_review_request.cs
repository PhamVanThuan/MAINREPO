using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Valuation_Review_Request.OnEnter
{
    [Subject("State => Valuation_Review_Request => OnEnter")] // AutoGenerated
    internal class when_valuation_review_request : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Valuation_Review_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}