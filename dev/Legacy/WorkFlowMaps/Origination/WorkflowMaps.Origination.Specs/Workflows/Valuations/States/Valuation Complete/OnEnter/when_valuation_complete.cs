using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Valuation_Complete.OnEnter
{
    [Subject("State => Valuation_Complete => OnEnter")] // AutoGenerated
    internal class when_valuation_complete : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Valuation_Complete(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}