using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Common_Set_Active_Valuation.OnExit
{
    [Subject("State => Common_Set_Active_Valuation => OnExit")] // AutoGenerated
    internal class when_common_set_active_valuation : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Common_Set_Active_Valuation(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}