using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Schedule_Valuation_Assessment.OnEnter
{
    [Subject("State => Schedule_Valuation_Assessment => OnEnter")] // AutoGenerated
    internal class when_schedule_valuation_assessment : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Schedule_Valuation_Assessment(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}