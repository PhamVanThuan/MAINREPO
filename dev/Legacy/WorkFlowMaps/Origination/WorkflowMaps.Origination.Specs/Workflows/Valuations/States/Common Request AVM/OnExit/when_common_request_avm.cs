using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Common_Request_AVM.OnExit
{
    [Subject("State => Common_Request_AVM => OnExit")] // AutoGenerated
    internal class when_common_request_avm : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Common_Request_AVM(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}