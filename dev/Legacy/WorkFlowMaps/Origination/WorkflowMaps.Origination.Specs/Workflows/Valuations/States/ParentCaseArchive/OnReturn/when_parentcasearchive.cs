using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.ParentCaseArchive.OnReturn
{
    [Subject("State => ParentCaseArchive => OnReturn")] // AutoGenerated
    internal class when_parentcasearchive : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnReturn_ParentCaseArchive(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}