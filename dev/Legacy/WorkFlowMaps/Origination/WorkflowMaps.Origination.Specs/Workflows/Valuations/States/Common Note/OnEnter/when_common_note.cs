using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Common_Note.OnEnter
{
    [Subject("State => Common_Note => OnEnter")] // AutoGenerated
    internal class when_common_note : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Common_Note(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}