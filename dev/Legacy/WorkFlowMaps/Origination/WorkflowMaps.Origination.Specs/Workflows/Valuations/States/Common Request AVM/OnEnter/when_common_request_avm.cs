using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Common_Request_AVM.OnEnter
{
    [Subject("State => Common_Request_AVM => OnEnter")] // AutoGenerated
    internal class when_common_request_avm : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Common_Request_AVM(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}