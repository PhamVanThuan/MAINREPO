using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Further_Valuation_Request.OnEnter
{
    [Subject("State => Further_Valuation_Request => OnEnter")] // AutoGenerated
    internal class when_further_valuation_request : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Further_Valuation_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}