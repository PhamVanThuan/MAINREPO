using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Manager_Archive.OnReturn
{
    [Subject("State => Manager_Archive => OnReturn")] // AutoGenerated
    internal class when_manager_archive : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnReturn_Manager_Archive(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}