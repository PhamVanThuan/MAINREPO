using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Manager_Archive.OnExit
{
    [Subject("State => Manager_Archive => OnExit")] // AutoGenerated
    internal class when_manager_archive : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Manager_Archive(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}