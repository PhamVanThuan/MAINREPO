using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Common_Note.OnExit
{
    [Subject("State => Common_Note => OnExit")] // AutoGenerated
    internal class when_common_note : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Common_Note(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}