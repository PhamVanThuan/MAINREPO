using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Check_Valuation.OnExit
{
    [Subject("State => Check_Valuation => OnExit")] // AutoGenerated
    internal class when_check_valuation : WorkflowSpecValuations
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnExit_Check_Valuation(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}