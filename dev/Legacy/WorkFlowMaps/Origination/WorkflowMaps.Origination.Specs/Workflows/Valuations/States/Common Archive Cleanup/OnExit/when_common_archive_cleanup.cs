using Machine.Specifications;

namespace WorkflowMaps.Valuations.Specs.States.Common_Archive_Cleanup.OnExit
{
    [Subject("State => Common_Archive_Cleanup => OnExit")] // AutoGenerated
    internal class when_common_archive_cleanup : WorkflowSpecValuations
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Common_Archive_Cleanup(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}