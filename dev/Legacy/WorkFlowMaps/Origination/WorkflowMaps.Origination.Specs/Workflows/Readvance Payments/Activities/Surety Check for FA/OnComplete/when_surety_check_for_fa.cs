using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Surety_Check_for_FA.OnComplete
{
    [Subject("Activity => Surety_Check_for_FA => OnComplete")] // AutoGenerated
    internal class when_surety_check_for_fa : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Surety_Check_for_FA(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}