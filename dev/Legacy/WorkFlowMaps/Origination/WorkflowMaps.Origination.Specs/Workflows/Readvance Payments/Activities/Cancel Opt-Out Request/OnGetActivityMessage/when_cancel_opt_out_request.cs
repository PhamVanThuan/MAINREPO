using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Cancel_Opt_Out_Request.OnGetActivityMessage
{
    [Subject("Activity => Cancel_Opt_Out_Request => OnGetActivityMessage")] // AutoGenerated
    internal class when_cancel_opt_out_request : WorkflowSpecReadvancePayments
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Cancel_Opt_Out_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}