using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Continue_with_Followup.OnStart
{
    [Subject("Activity => Continue_with_Followup => OnStart")] // AutoGenerated
    internal class when_continue_with_followup : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Continue_with_Followup(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}