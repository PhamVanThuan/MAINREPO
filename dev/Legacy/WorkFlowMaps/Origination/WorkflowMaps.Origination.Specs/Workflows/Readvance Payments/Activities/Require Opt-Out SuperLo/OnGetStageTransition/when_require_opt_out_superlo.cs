using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Require_Opt_Out_SuperLo.OnGetStageTransition
{
    [Subject("Activity => Require_Opt_Out_SuperLo => OnGetStageTransition")] // AutoGenerated
    internal class when_require_opt_out_superlo : WorkflowSpecReadvancePayments
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Require_Opt_Out_SuperLo(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}