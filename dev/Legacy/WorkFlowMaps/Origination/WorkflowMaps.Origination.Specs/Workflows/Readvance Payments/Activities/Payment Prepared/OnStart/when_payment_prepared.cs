using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Payment_Prepared.OnStart
{
    [Subject("Activity => Payment_Prepared => OnStart")] // AutoGenerated
    internal class when_payment_prepared : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Payment_Prepared(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}