using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Disbursement_Incorrect.OnGetActivityMessage
{
    [Subject("Activity => Disbursement_Incorrect => OnGetActivityMessage")] // AutoGenerated
    internal class when_disbursement_incorrect : WorkflowSpecReadvancePayments
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Disbursement_Incorrect(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}