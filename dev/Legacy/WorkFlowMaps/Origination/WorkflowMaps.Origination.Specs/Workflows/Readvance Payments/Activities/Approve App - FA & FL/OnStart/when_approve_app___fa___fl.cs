using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Approve_App___FA___FL.OnStart
{
    [Subject("Activity => Approve_App___FA___FL => OnStart")] // AutoGenerated
    internal class when_approve_app___fa___fl : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Approve_App___FA___FL(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}