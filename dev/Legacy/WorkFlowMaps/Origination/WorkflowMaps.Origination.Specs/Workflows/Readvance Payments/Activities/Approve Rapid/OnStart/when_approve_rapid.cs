using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Approve_Rapid.OnStart
{
    [Subject("Activity => Approve_Rapid => OnStart")] // AutoGenerated
    internal class when_approve_rapid : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Approve_Rapid(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}