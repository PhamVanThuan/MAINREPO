using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Receive_Schedule.OnStart
{
    [Subject("Activity => Receive_Schedule => OnStart")] // AutoGenerated
    internal class when_receive_schedule : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Receive_Schedule(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}