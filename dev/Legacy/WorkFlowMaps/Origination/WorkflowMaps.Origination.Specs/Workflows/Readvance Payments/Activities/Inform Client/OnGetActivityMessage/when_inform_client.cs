using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Inform_Client.OnGetActivityMessage
{
    [Subject("Activity => Inform_Client => OnGetActivityMessage")] // AutoGenerated
    internal class when_inform_client : WorkflowSpecReadvancePayments
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Inform_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}