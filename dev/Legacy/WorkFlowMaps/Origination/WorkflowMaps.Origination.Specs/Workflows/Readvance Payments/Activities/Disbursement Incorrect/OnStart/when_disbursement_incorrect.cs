using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Disbursement_Incorrect.OnStart
{
    [Subject("Activity => Disbursement_Incorrect => OnStart")] // AutoGenerated
    internal class when_disbursement_incorrect : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Disbursement_Incorrect(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}