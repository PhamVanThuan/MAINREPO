using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Reinstate_Followup.OnComplete
{
    [Subject("Activity => Reinstate_Followup => OnComplete")] // AutoGenerated
    internal class when_reinstate_followup : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Reinstate_Followup(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}