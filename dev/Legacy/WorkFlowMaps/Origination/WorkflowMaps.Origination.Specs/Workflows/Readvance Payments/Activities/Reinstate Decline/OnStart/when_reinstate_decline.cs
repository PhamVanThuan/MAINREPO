using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Reinstate_Decline.OnStart
{
    [Subject("Activity => Reinstate_Decline => OnStart")] // AutoGenerated
    internal class when_reinstate_decline : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Reinstate_Decline(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}