using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Cancel_Opt_Out_Request.OnStart
{
    [Subject("Activity => Cancel_Opt_Out_Request => OnStart")] // AutoGenerated
    internal class when_cancel_opt_out_request : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Cancel_Opt_Out_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}