using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Approve_Rapid.OnGetActivityMessage
{
    [Subject("Activity => Approve_Rapid => OnGetActivityMessage")] // AutoGenerated
    internal class when_approve_rapid : WorkflowSpecReadvancePayments
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Approve_Rapid(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}