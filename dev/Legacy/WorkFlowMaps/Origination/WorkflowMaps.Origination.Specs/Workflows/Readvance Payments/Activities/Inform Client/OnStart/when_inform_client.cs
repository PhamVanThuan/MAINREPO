using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Inform_Client.OnStart
{
    [Subject("Activity => Inform_Client => OnStart")] // AutoGenerated
    internal class when_inform_client : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Inform_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}