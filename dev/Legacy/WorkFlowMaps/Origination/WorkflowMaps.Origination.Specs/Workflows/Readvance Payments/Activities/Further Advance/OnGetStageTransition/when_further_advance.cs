using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Further_Advance.OnGetStageTransition
{
    [Subject("Activity => Further_Advance => OnGetStageTransition")] // AutoGenerated
    internal class when_further_advance : WorkflowSpecReadvancePayments
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Further_Advance(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}