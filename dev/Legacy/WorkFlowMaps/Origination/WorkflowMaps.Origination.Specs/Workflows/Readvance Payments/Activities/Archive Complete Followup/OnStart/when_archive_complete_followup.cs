using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Archive_Complete_Followup.OnStart
{
    [Subject("Activity => Archive_Complete_Followup => OnStart")] // AutoGenerated
    internal class when_archive_complete_followup : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Archive_Complete_Followup(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}