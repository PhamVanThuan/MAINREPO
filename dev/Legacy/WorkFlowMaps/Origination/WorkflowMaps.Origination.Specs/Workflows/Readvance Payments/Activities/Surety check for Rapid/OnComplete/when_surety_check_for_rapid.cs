using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Surety_check_for_Rapid.OnComplete
{
    [Subject("Activity => Surety_check_for_Rapid => OnComplete")] // AutoGenerated
    internal class when_surety_check_for_rapid : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Surety_check_for_Rapid(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}