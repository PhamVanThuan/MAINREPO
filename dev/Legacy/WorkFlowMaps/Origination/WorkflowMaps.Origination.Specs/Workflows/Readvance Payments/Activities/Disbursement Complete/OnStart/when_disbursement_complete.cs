using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Disbursement_Complete.OnStart
{
    [Subject("Activity => Disbursement_Complete => OnStart")] // AutoGenerated
    internal class when_disbursement_complete : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Disbursement_Complete(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}