using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.OnFollowUpReturn.OnStart
{
    [Subject("Activity => OnFollowUpReturn => OnStart")] // AutoGenerated
    internal class when_onfollowupreturn : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_OnFollowUpReturn(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}