using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.Activities.Opt_Out_SuperLo.OnGetActivityMessage
{
    [Subject("Activity => Opt_Out_SuperLo => OnGetActivityMessage")] // AutoGenerated
    internal class when_opt_out_superlo : WorkflowSpecReadvancePayments
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Opt_Out_SuperLo(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeEmpty();
        };
    }
}