using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.State29.OnEnter
{
    [Subject("State => State29 => OnEnter")] // AutoGenerated
    internal class when_state29 : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_State29(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}