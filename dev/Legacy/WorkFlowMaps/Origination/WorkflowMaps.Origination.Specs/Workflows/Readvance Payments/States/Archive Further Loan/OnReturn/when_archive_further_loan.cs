using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.Archive_Further_Loan.OnReturn
{
    [Subject("State => Archive_Further_Loan => OnReturn")] // AutoGenerated
    internal class when_archive_further_loan : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnReturn_Archive_Further_Loan(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}