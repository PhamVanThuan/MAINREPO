using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.OptOut_Return.OnEnter
{
    [Subject("State => OptOut_Return => OnEnter")] // AutoGenerated
    internal class when_optout_return : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_OptOut_Return(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}