using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.Check_Application.OnExit
{
    [Subject("State => Check_Application => OnExit")] // AutoGenerated
    internal class when_check_application : WorkflowSpecReadvancePayments
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnExit_Check_Application(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}