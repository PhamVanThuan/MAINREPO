using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.FollowupReturn.OnAutoForward
{
    [Subject("State => FollowupReturn => OnAutoForward")] // AutoGenerated
    internal class when_followupreturn : WorkflowSpecReadvancePayments
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetForwardStateName_FollowupReturn(messages, workflowData, instanceData, paramsData);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}