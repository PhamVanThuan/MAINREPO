using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.Followup_Complete.OnExit
{
    [Subject("State => Followup_Complete => OnExit")] // AutoGenerated
    internal class when_followup_complete : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Followup_Complete(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}