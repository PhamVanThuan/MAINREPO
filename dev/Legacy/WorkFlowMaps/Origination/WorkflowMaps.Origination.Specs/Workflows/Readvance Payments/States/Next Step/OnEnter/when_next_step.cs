using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.Next_Step.OnEnter
{
    [Subject("State => Next_Step => OnEnter")] // AutoGenerated
    internal class when_next_step : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Next_Step(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}