using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.Awaiting_Schedule.OnEnter
{
    [Subject("State => Awaiting_Schedule => OnEnter")] // AutoGenerated
    internal class when_awaiting_schedule : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Awaiting_Schedule(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}