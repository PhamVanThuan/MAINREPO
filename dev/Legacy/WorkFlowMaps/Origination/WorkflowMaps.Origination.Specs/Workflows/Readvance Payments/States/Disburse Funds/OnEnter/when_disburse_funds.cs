using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.Disburse_Funds.OnEnter
{
    [Subject("State => Disburse_Funds => OnEnter")] // AutoGenerated
    internal class when_disburse_funds : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Disburse_Funds(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}