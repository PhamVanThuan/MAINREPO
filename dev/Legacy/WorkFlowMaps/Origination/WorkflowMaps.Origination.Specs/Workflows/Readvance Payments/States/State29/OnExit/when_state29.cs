using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.State29.OnExit
{
    [Subject("State => State29 => OnExit")] // AutoGenerated
    internal class when_state29 : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_State29(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}