using Machine.Specifications;

namespace WorkflowMaps.ReadvancePayments.Specs.States.Setup_Payment.OnEnter
{
    [Subject("State => Setup_Payment => OnEnter")] // AutoGenerated
    internal class when_setup_payment : WorkflowSpecReadvancePayments
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Setup_Payment(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}