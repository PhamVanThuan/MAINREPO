using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Override_Check.OnGetActivityMessage
{
    [Subject("Activity => Override_Check => OnGetActivityMessage")] // AutoGenerated
    internal class when_override_check : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Override_Check(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}