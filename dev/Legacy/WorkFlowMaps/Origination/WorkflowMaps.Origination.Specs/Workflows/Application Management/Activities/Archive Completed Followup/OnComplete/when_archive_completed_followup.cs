using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Archive_Completed_Followup.OnComplete
{
    [Subject("Activity => Archive_Completed_Followup => OnComplete")] // AutoGenerated
    internal class when_archive_completed_followup : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Archive_Completed_Followup(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}