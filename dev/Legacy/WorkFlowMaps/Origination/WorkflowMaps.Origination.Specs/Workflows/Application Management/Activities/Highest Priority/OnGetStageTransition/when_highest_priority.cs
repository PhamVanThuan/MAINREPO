using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Highest_Priority.OnGetStageTransition
{
    [Subject("Activity => Highest_Priority => OnGetStageTransition")] // AutoGenerated
    internal class when_highest_priority : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Highest_Priority(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}