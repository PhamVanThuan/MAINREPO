using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Application_Received.OnGetStageTransition
{
    [Subject("Activity => Application_Received => OnGetStageTransition")] // AutoGenerated
    internal class when_application_received : WorkflowSpecApplicationManagement
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_Application_Received(messages, workflowData, instanceData, paramsData);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}