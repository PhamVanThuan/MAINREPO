using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Credit_Return_Resub_Approve.OnStart
{
    [Subject("Activity => Credit_Return_Resub_Approve => OnStart")] // AutoGenerated
    internal class when_credit_return_resub_approve : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Credit_Return_Resub_Approve(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}