using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Request_Resolved.OnGetActivityMessage
{
    [Subject("Activity => Request_Resolved => OnGetActivityMessage")] // AutoGenerated
    internal class when_request_resolved : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Request_Resolved(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}