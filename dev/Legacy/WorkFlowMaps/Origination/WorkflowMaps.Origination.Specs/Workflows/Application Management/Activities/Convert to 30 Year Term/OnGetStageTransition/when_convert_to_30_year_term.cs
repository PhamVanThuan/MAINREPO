using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Convert_to_30_Year_Term.OnGetStageTransition
{
    [Subject("Activity => Convert_to_30_Year_Term => OnGetStageTransition")] // AutoGenerated
    internal class when_convert_to_30_year_term : WorkflowSpecApplicationManagement
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_Convert_to_30_Year_Term(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}