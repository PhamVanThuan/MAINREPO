using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Revert_to_Previous_Term.OnGetActivityMessage
{
    [Subject("Activity => Revert_to_Previous_Term => OnGetActivityMessage")] // AutoGenerated
    internal class when_revert_to_previous_term : WorkflowSpecApplicationManagement
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetActivityMessage_Revert_to_Previous_Term(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}