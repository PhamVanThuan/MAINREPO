using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Convert_to_30_Year_Term.OnComplete
{
    [Subject("Activity => Convert_to_30_Year_Term => OnComplete")] // AutoGenerated
    internal class when_convert_to_30_year_term : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
			string message = string.Empty;
            result = workflow.OnCompleteActivity_Convert_to_30_Year_Term(instanceData, workflowData, paramsData, messages, ref message);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}