using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.OnReturnReviewVal.OnStart
{
    [Subject("Activity => OnReturnReviewVal => OnStart")] // AutoGenerated
    internal class when_onreturnreviewval : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_OnReturnReviewVal(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}