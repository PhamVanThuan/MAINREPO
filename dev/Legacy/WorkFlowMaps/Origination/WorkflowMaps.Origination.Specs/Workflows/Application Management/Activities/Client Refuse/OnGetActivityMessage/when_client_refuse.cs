using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Client_Refuse.OnGetActivityMessage
{
    [Subject("Activity => Client_Refuse => OnGetActivityMessage")] // AutoGenerated
    internal class when_client_refuse : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Client_Refuse(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}