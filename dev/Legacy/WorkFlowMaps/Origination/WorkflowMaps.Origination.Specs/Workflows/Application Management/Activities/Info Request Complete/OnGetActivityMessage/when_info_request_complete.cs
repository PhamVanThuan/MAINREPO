using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Info_Request_Complete.OnGetActivityMessage
{
    [Subject("Activity => Info_Request_Complete => OnGetActivityMessage")] // AutoGenerated
    internal class when_info_request_complete : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Info_Request_Complete(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}