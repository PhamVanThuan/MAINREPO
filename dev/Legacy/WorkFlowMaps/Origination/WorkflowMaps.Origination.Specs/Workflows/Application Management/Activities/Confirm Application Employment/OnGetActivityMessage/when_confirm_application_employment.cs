using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Confirm_Application_Employment.OnGetActivityMessage
{
    [Subject("Activity => Confirm_Application_Employment => OnGetActivityMessage")] // AutoGenerated
    internal class when_confirm_application_employment : WorkflowSpecApplicationManagement
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetActivityMessage_Confirm_Application_Employment(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}