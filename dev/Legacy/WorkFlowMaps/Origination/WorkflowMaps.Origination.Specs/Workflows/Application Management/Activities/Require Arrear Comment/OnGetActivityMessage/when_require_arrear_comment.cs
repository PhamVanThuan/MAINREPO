using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Require_Arrear_Comment.OnGetActivityMessage
{
    [Subject("Activity => Require_Arrear_Comment => OnGetActivityMessage")] // AutoGenerated
    internal class when_require_arrear_comment : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Require_Arrear_Comment(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}