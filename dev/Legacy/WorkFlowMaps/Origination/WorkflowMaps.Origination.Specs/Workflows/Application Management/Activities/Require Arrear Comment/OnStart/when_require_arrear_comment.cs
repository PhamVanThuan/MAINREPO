using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Require_Arrear_Comment.OnStart
{
    [Subject("Activity => Require_Arrear_Comment => OnStart")] // AutoGenerated
    internal class when_require_arrear_comment : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Require_Arrear_Comment(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}