using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Reset_30_Day_Timer.OnStart
{
    [Subject("Activity => Reset_30_Day_Timer => OnStart")] // AutoGenerated
    internal class when_reset_30_day_timer : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_Reset_30_Day_Timer(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}