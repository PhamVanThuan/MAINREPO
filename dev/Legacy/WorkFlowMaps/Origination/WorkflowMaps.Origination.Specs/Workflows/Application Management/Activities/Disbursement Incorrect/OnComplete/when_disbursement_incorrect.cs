using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Disbursement_Incorrect.OnComplete
{
    [Subject("Activity => Disbursement_Incorrect => OnComplete")] // AutoGenerated
    internal class when_disbursement_incorrect : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Disbursement_Incorrect(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}