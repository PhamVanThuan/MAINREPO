using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities._30_Year_Term_Detail.OnComplete
{
    [Subject("Activity => 30_Year_Term_Detail => OnComplete")] // AutoGenerated
    internal class when_30_year_term_detail : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
			string message = string.Empty;
            result = workflow.OnCompleteActivity_30_Year_Term_Detail(instanceData, workflowData, paramsData, messages, ref message);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}