using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.EXT_DataStore_FL_Doc_Recieved.OnComplete
{
    [Subject("Activity => EXT_DataStore_FL_Doc_Recieved => OnComplete")] // AutoGenerated
    internal class when_ext_datastore_fl_doc_recieved : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_EXT_DataStore_FL_Doc_Recieved(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}