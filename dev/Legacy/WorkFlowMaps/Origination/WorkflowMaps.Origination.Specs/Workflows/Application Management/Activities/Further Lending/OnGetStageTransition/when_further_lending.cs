using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Further_Lending.OnGetStageTransition
{
    [Subject("Activity => Further_Lending => OnGetStageTransition")] // AutoGenerated
    internal class when_further_lending : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Further_Lending(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}