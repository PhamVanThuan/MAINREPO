using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Send_Feedback.OnGetStageTransition
{
    [Subject("Activity => Send_Feedback => OnGetStageTransition")] // AutoGenerated
    internal class when_send_feedback : WorkflowSpecApplicationManagement
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_Send_Feedback(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}