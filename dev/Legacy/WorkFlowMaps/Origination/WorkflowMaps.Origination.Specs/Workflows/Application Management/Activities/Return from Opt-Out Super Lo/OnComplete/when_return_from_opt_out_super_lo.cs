using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Return_from_Opt_Out_Super_Lo.OnComplete
{
    [Subject("Activity => Return_from_Opt_Out_Super_Lo => OnComplete")] // AutoGenerated
    internal class when_return_from_opt_out_super_lo : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Return_from_Opt_Out_Super_Lo(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}