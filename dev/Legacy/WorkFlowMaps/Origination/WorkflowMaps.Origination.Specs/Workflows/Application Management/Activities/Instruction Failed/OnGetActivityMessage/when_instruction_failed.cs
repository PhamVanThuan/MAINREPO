using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Instruction_Failed.OnGetActivityMessage
{
    [Subject("Activity => Instruction_Failed => OnGetActivityMessage")] // AutoGenerated
    internal class when_instruction_failed : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Instruction_Failed(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}