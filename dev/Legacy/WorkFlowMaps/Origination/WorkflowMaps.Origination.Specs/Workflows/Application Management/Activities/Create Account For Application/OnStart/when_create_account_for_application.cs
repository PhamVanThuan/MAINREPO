using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Create_Account_For_Application.OnStart
{
    [Subject("Activity => Create_Account_For_Application => OnStart")] // AutoGenerated
    internal class when_create_account_for_application : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Create_Account_For_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}