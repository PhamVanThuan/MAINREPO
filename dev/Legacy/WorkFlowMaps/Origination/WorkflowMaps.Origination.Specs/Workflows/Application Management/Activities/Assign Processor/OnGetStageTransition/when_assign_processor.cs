using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Assign_Processor.OnGetStageTransition
{
    [Subject("Activity => Assign_Processor => OnGetStageTransition")] // AutoGenerated
    internal class when_assign_processor : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Assign_Processor(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}