using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Return_Credit_Pre_Approved.OnComplete
{
    [Subject("Activity => Return_Credit_Pre_Approved => OnComplete")] // AutoGenerated
    internal class when_return_credit_pre_approved : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Return_Credit_Pre_Approved(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}