using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Credit_Return_Resubmission.OnComplete
{
    [Subject("Activity => Credit_Return_Resubmission => OnComplete")] // AutoGenerated
    internal class when_credit_return_resubmission : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Credit_Return_Resubmission(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}