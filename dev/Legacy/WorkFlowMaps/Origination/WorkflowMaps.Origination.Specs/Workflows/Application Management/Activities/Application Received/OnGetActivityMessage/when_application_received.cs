using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Application_Received.OnGetActivityMessage
{
    [Subject("Activity => Application_Received => OnGetActivityMessage")] // AutoGenerated
    internal class when_application_received : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Application_Received(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}