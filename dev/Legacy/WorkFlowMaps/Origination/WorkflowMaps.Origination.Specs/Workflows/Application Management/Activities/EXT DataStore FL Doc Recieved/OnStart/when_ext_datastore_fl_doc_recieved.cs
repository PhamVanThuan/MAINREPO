using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.EXT_DataStore_FL_Doc_Recieved.OnStart
{
    [Subject("Activity => EXT_DataStore_FL_Doc_Recieved => OnStart")] // AutoGenerated
    internal class when_ext_datastore_fl_doc_recieved : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_EXT_DataStore_FL_Doc_Recieved(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}