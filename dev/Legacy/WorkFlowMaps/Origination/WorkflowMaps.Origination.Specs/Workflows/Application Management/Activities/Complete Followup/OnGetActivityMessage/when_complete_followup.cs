using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Complete_Followup.OnGetActivityMessage
{
    [Subject("Activity => Complete_Followup => OnGetActivityMessage")] // AutoGenerated
    internal class when_complete_followup : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Complete_Followup(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}