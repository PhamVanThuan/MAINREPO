using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Client_Accepts.OnStart
{
    [Subject("Activity => Client_Accepts => OnStart")] // AutoGenerated
    internal class when_client_accepts : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Client_Accepts(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}