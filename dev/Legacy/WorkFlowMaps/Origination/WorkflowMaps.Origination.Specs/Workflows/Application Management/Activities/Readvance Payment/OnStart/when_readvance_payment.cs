using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Readvance_Payment.OnStart
{
    [Subject("Activity => Readvance_Payment => OnStart")] // AutoGenerated
    internal class when_readvance_payment : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Readvance_Payment(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}