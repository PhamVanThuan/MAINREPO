using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Feedback_on_Query.OnGetActivityMessage
{
    [Subject("Activity => Feedback_on_Query => OnGetActivityMessage")] // AutoGenerated
    internal class when_feedback_on_query : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Feedback_on_Query(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}