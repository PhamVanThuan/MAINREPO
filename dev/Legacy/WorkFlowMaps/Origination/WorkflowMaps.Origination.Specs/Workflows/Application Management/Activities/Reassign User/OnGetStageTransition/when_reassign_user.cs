using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Reassign_User.OnGetStageTransition
{
    [Subject("Activity => Reassign_User => OnGetStageTransition")] // AutoGenerated
    internal class when_reassign_user : WorkflowSpecApplicationManagement
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_Reassign_User(messages, workflowData, instanceData, paramsData);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}