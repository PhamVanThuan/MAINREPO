using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Rapid_Readvance.OnGetStageTransition
{
    [Subject("Activity => Rapid_Readvance => OnGetStageTransition")] // AutoGenerated
    internal class when_rapid_readvance : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Rapid_Readvance(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}