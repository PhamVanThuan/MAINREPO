using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Return_ReAdv_Payment_FL.OnStart
{
    [Subject("Activity => Return_ReAdv_Payment_FL => OnStart")] // AutoGenerated
    internal class when_return_readv_payment_fl : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Return_ReAdv_Payment_FL(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}