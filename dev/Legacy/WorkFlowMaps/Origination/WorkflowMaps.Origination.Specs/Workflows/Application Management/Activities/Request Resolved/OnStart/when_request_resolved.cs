using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Request_Resolved.OnStart
{
    [Subject("Activity => Request_Resolved => OnStart")] // AutoGenerated
    internal class when_request_resolved : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Request_Resolved(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}