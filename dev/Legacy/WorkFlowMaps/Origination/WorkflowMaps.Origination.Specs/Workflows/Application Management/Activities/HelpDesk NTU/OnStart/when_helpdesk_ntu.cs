using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.HelpDesk_NTU.OnStart
{
    [Subject("Activity => HelpDesk_NTU => OnStart")] // AutoGenerated
    internal class when_helpdesk_ntu : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_HelpDesk_NTU(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}