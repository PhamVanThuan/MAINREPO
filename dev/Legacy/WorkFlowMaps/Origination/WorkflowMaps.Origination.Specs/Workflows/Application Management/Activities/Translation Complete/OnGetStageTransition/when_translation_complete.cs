using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Translation_Complete.OnGetStageTransition
{
    [Subject("Activity => Translation_Complete => OnGetStageTransition")] // AutoGenerated
    internal class when_translation_complete : WorkflowSpecApplicationManagement
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_Translation_Complete(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs("Translation Complete");
        };
    }
}