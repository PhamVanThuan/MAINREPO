using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Perform_Valuation.OnGetActivityMessage
{
    [Subject("Activity => Perform_Valuation => OnGetActivityMessage")] // AutoGenerated
    internal class when_perform_valuation : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Perform_Valuation(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}