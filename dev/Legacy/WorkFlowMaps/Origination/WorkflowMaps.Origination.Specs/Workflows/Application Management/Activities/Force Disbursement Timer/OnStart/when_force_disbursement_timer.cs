using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Force_Disbursement_Timer.OnStart
{
    [Subject("Activity => Force_Disbursement_Timer => OnStart")] // AutoGenerated
    internal class when_force_disbursement_timer : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Force_Disbursement_Timer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}