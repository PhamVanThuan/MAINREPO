using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Proceed_with_Application.OnGetStageTransition
{
    [Subject("Activity => Proceed_with_Application => OnGetStageTransition")] // AutoGenerated
    internal class when_proceed_with_application : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Proceed_with_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}