using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Return_with_Approve_or_Offer.OnComplete
{
    [Subject("Activity => Return_with_Approve_or_Offer => OnComplete")] // AutoGenerated
    internal class when_return_with_approve_or_offer : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Return_with_Approve_or_Offer(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}