using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Return_with_Approve_or_Offer.OnStart
{
    [Subject("Activity => Return_with_Approve_or_Offer => OnStart")] // AutoGenerated
    internal class when_return_with_approve_or_offer : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Return_with_Approve_or_Offer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}