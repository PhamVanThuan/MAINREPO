using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.EXT_CommonArchiveMain.OnStart
{
    [Subject("Activity => EXT_CommonArchiveMain => OnStart")] // AutoGenerated
    internal class when_ext_commonarchivemain : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_EXT_CommonArchiveMain(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}