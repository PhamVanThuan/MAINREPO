using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Create_Account_For_Application.OnGetActivityMessage
{
    [Subject("Activity => Create_Account_For_Application => OnGetActivityMessage")] // AutoGenerated
    internal class when_create_account_for_application : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Create_Account_For_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}