using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Pipeline_UpForFees.OnStart
{
    [Subject("Activity => Pipeline_UpForFees => OnStart")] // AutoGenerated
    internal class when_pipeline_upforfees : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Pipeline_UpForFees(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}