using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Translation_Complete.OnStart
{
    [Subject("Activity => Translation_Complete => OnStart")] // AutoGenerated
    internal class when_translation_complete : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_Translation_Complete(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}