using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Override_Check.OnStart
{
    [Subject("Activity => Override_Check => OnStart")] // AutoGenerated
    internal class when_override_check : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Override_Check(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}