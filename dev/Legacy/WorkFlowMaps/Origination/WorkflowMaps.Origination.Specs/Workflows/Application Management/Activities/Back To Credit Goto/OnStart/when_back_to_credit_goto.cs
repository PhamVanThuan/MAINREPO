using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Back_To_Credit_Goto.OnStart
{
    [Subject("Activity => Back_To_Credit_Goto => OnStart")] // AutoGenerated
    internal class when_back_to_credit_goto : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Back_To_Credit_Goto(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}