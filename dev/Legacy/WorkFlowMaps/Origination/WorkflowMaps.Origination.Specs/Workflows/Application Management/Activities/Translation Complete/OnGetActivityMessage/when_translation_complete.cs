using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Translation_Complete.OnGetActivityMessage
{
    [Subject("Activity => Translation_Complete => OnGetActivityMessage")] // AutoGenerated
    internal class when_translation_complete : WorkflowSpecApplicationManagement
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetActivityMessage_Translation_Complete(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}