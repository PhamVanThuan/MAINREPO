using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Create_EWork_PipelineCase.OnComplete
{
    [Subject("Activity => Create_EWork_PipelineCase => OnComplete")] // AutoGenerated
    internal class when_create_ework_pipelinecase : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Create_EWork_PipelineCase(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}