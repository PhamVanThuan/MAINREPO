using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.ReAssign_Commission_Consultant.OnGetStageTransition
{
    [Subject("Activity => ReAssign_Commission_Consultant => OnGetStageTransition")] // AutoGenerated
    internal class when_reassign_commission_consultant : WorkflowSpecApplicationManagement
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_ReAssign_Commission_Consultant(messages, workflowData, instanceData, paramsData);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}