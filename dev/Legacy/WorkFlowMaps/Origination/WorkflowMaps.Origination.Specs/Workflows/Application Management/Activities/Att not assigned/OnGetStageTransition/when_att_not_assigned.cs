using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Att_not_assigned.OnGetStageTransition
{
    [Subject("Activity => Att_not_assigned => OnGetStageTransition")] // AutoGenerated
    internal class when_att_not_assigned : WorkflowSpecApplicationManagement
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Att_not_assigned(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}