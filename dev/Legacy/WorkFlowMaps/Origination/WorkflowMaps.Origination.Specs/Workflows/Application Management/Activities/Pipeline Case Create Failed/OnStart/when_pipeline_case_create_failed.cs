using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.Activities.Pipeline_Case_Create_Failed.OnStart
{
    [Subject("Activity => Pipeline_Case_Create_Failed => OnStart")] // AutoGenerated
    internal class when_pipeline_case_create_failed : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Pipeline_Case_Create_Failed(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}