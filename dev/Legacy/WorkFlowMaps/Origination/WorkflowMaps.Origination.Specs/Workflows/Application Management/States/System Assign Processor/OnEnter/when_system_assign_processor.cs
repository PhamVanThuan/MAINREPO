using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.System_Assign_Processor.OnEnter
{
    [Subject("State => System_Assign_Processor => OnEnter")] // AutoGenerated
    internal class when_system_assign_processor : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_System_Assign_Processor(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}