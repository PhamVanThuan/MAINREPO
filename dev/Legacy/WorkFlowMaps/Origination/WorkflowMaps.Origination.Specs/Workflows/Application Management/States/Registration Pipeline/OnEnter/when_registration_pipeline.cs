using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Registration_Pipeline.OnEnter
{
    [Subject("State => Registration_Pipeline => OnEnter")] // AutoGenerated
    internal class when_registration_pipeline : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Registration_Pipeline(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}