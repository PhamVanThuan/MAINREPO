using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Disbursement_Review.OnEnter
{
    [Subject("State => Disbursement_Review => OnEnter")] // AutoGenerated
    internal class when_disbursement_review : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Disbursement_Review(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}