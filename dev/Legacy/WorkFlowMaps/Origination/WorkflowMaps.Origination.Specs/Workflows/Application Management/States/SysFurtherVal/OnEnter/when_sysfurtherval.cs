using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.SysFurtherVal.OnEnter
{
    [Subject("State => SysFurtherVal => OnEnter")] // AutoGenerated
    internal class when_sysfurtherval : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_SysFurtherVal(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}