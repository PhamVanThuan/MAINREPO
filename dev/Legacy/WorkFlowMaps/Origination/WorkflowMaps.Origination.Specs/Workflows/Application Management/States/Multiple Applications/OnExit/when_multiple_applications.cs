using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Multiple_Applications.OnExit
{
    [Subject("State => Multiple_Applications => OnExit")] // AutoGenerated
    internal class when_multiple_applications : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Multiple_Applications(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}