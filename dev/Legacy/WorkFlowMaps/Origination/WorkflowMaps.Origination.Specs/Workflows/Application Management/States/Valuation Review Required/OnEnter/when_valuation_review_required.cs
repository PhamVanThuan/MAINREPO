using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Valuation_Review_Required.OnEnter
{
    [Subject("State => Valuation_Review_Required => OnEnter")] // AutoGenerated
    internal class when_valuation_review_required : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Valuation_Review_Required(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}