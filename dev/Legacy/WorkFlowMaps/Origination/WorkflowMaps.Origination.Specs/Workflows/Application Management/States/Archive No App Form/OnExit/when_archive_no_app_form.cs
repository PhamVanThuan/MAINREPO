using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Archive_No_App_Form.OnExit
{
    [Subject("State => Archive_No_App_Form => OnExit")] // AutoGenerated
    internal class when_archive_no_app_form : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Archive_No_App_Form(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}