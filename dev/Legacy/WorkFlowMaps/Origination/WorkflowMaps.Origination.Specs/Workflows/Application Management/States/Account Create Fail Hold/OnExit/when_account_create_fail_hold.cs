using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Account_Create_Fail_Hold.OnExit
{
    [Subject("State => Account_Create_Fail_Hold => OnExit")] // AutoGenerated
    internal class when_account_create_fail_hold : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Account_Create_Fail_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}