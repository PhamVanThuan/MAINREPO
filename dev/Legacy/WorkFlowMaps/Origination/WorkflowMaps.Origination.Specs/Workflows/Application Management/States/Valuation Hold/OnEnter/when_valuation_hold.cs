using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Valuation_Hold.OnEnter
{
    [Subject("State => Valuation_Hold => OnEnter")] // AutoGenerated
    internal class when_valuation_hold : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Valuation_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}