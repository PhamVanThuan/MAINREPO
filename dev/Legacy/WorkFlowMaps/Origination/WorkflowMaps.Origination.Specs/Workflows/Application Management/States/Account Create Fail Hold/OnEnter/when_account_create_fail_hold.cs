using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Account_Create_Fail_Hold.OnEnter
{
    [Subject("State => Account_Create_Fail_Hold => OnEnter")] // AutoGenerated
    internal class when_account_create_fail_hold : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Account_Create_Fail_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}