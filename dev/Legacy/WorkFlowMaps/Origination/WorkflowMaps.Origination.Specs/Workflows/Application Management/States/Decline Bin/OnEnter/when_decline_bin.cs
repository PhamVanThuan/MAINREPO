using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Decline_Bin.OnEnter
{
    [Subject("State => Decline_Bin => OnEnter")] // AutoGenerated
    internal class when_decline_bin : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = true;
        };
		
        Because of = () =>
        {
            result = workflow.OnEnter_Decline_Bin(messages, workflowData, instanceData, paramsData);
        };

        It should_return_false = () =>
        {
            result.ShouldBeFalse();
        };
    }
}