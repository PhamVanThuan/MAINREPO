using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.CheckQCAndSendToPipeline.OnExit
{
    [Subject("State => CheckQCAndSendToPipeline => OnExit")] // AutoGenerated
    internal class when_checkqcandsendtopipeline : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_CheckQCAndSendToPipeline(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}