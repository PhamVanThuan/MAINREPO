using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.CheckQCAndSendToPipeline.OnEnter
{
    [Subject("State => CheckQCAndSendToPipeline => OnEnter")] // AutoGenerated
    internal class when_checkqcandsendtopipeline : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_CheckQCAndSendToPipeline(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}