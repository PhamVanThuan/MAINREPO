using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.HelpDesk_NTU.OnExit
{
    [Subject("State => HelpDesk_NTU => OnExit")] // AutoGenerated
    internal class when_helpdesk_ntu : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnExit_HelpDesk_NTU(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}