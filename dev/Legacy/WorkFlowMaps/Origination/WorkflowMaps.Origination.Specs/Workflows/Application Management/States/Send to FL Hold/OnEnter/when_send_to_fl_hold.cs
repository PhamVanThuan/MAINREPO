using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Send_to_FL_Hold.OnEnter
{
    [Subject("State => Send_to_FL_Hold => OnEnter")] // AutoGenerated
    internal class when_send_to_fl_hold : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Send_to_FL_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}