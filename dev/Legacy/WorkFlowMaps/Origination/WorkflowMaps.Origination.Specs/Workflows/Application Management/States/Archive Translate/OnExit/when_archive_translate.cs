using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Archive_Translate.OnExit
{
    [Subject("State => Archive_Translate => OnExit")] // AutoGenerated
    internal class when_archive_translate : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnExit_Archive_Translate(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}