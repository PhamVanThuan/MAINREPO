using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Archive_Translate.OnReturn
{
    [Subject("State => Archive_Translate => OnReturn")] // AutoGenerated
    internal class when_archive_translate : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnReturn_Archive_Translate(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}