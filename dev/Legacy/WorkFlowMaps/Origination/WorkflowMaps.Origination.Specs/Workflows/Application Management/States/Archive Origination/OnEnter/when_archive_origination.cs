using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Archive_Origination.OnEnter
{
    [Subject("State => Archive_Origination => OnEnter")] // AutoGenerated
    internal class when_archive_origination : WorkflowSpecApplicationManagement
    {
        static bool result;
        Establish context = () =>
        {
            result = true;
        };
		
        Because of = () =>
        {
            result = workflow.OnEnter_Archive_Origination(messages, workflowData, instanceData, paramsData);
        };

        It should_return_false = () =>
        {
            result.ShouldBeFalse();
        };
    }
}