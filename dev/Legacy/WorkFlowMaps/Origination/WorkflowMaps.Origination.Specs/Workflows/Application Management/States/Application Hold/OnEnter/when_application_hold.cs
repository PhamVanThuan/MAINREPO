using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Application_Hold.OnEnter
{
    [Subject("State => Application_Hold => OnEnter")] // AutoGenerated
    internal class when_application_hold : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Application_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}