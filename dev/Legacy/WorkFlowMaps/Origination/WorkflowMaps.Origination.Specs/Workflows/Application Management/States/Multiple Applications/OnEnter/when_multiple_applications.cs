using Machine.Specifications;

namespace WorkflowMaps.ApplicationManagement.Specs.States.Multiple_Applications.OnEnter
{
    [Subject("State => Multiple_Applications => OnEnter")] // AutoGenerated
    internal class when_multiple_applications : WorkflowSpecApplicationManagement
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Multiple_Applications(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}