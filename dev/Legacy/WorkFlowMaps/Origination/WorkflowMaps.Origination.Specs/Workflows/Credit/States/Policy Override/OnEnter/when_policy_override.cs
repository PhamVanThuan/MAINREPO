using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Policy_Override.OnEnter
{
    [Subject("State => Policy_Override => OnEnter")] // AutoGenerated
    internal class when_policy_override : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Policy_Override(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}