using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Archive_Further_Info.OnReturn
{
    [Subject("State => Archive_Further_Info => OnReturn")] // AutoGenerated
    internal class when_archive_further_info : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnReturn_Archive_Further_Info(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}