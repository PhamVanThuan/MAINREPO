using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Archive_Disputes.OnExit
{
    [Subject("State => Archive_Disputes => OnExit")] // AutoGenerated
    internal class when_archive_disputes : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Archive_Disputes(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}