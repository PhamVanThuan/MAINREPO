using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Common_Dispute.OnEnter
{
    [Subject("State => Common_Dispute => OnEnter")] // AutoGenerated
    internal class when_common_dispute : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Common_Dispute(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}