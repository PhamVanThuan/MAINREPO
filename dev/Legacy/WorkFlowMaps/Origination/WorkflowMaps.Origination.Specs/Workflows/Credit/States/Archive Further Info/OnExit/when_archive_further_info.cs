using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Archive_Further_Info.OnExit
{
    [Subject("State => Archive_Further_Info => OnExit")] // AutoGenerated
    internal class when_archive_further_info : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Archive_Further_Info(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}