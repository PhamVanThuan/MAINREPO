using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Archive_Apr_Offer_App.OnExit
{
    [Subject("State => Archive_Apr_Offer_App => OnExit")] // AutoGenerated
    internal class when_archive_apr_offer_app : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Archive_Apr_Offer_App(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}