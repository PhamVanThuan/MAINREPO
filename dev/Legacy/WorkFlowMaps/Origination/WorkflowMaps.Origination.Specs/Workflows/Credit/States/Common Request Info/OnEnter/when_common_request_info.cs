using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Common_Request_Info.OnEnter
{
    [Subject("State => Common_Request_Info => OnEnter")] // AutoGenerated
    internal class when_common_request_info : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Common_Request_Info(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}