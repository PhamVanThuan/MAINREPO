using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Valuation_Approval_Required.OnExit
{
    [Subject("State => Valuation_Approval_Required => OnExit")] // AutoGenerated
    internal class when_valuation_approval_required : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Valuation_Approval_Required(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}