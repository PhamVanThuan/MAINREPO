using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Review_Check.OnEnter
{
    [Subject("State => Review_Check => OnEnter")] // AutoGenerated
    internal class when_review_check : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Review_Check(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}