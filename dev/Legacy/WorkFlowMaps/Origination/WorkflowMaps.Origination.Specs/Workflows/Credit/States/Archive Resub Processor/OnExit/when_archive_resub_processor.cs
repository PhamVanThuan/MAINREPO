using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Archive_Resub_Processor.OnExit
{
    [Subject("State => Archive_Resub_Processor => OnExit")] // AutoGenerated
    internal class when_archive_resub_processor : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Archive_Resub_Processor(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}