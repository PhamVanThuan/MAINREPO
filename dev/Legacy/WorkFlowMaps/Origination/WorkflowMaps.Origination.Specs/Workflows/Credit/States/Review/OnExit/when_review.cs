using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Review.OnExit
{
    [Subject("State => Review => OnExit")] // AutoGenerated
    internal class when_review : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Review(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}