using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Credit_Create.OnEnter
{
    [Subject("State => Credit_Create => OnEnter")] // AutoGenerated
    internal class when_credit_create : WorkflowSpecCredit
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnEnter_Credit_Create(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}