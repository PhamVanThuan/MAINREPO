using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Archive_Approve_Resub.OnReturn
{
    [Subject("State => Archive_Approve_Resub => OnReturn")] // AutoGenerated
    internal class when_archive_approve_resub : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnReturn_Archive_Approve_Resub(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}