using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Common_Pricing_Changes.OnExit
{
    [Subject("State => Common_Pricing_Changes => OnExit")] // AutoGenerated
    internal class when_common_pricing_changes : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Common_Pricing_Changes(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}