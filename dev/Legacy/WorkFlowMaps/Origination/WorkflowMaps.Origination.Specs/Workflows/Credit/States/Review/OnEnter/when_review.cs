using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Review.OnEnter
{
    [Subject("State => Review => OnEnter")] // AutoGenerated
    internal class when_review : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Review(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}