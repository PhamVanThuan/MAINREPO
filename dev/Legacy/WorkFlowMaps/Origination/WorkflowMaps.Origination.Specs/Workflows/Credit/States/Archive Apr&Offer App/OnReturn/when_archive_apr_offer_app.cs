using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.Archive_Apr_Offer_App.OnReturn
{
    [Subject("State => Archive_Apr_Offer_App => OnReturn")] // AutoGenerated
    internal class when_archive_apr_offer_app : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnReturn_Archive_Apr_Offer_App(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}