using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.States.App_Check.OnEnter
{
    [Subject("State => App_Check => OnEnter")] // AutoGenerated
    internal class when_app_check : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_App_Check(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}