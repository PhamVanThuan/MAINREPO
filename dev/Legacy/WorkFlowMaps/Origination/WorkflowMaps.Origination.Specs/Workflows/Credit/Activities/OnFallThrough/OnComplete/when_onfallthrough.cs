using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.OnFallThrough.OnComplete
{
    [Subject("Activity => OnFallThrough => OnComplete")] // AutoGenerated
    internal class when_onfallthrough : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_OnFallThrough(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}