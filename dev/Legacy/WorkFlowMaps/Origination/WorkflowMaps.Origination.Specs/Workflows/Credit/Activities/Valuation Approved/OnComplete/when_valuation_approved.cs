using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Valuation_Approved.OnComplete
{
    [Subject("Activity => Valuation_Approved => OnComplete")] // AutoGenerated
    internal class when_valuation_approved : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Valuation_Approved(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}