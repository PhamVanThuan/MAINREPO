using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Request_Policy_Override.OnComplete
{
    [Subject("Activity => Request_Policy_Override => OnComplete")] // AutoGenerated
    internal class when_request_policy_override : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Request_Policy_Override(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}