using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Return_to_Processor.OnGetActivityMessage
{
    [Subject("Activity => Return_to_Processor => OnGetActivityMessage")] // AutoGenerated
    internal class when_return_to_processor : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Return_to_Processor(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}