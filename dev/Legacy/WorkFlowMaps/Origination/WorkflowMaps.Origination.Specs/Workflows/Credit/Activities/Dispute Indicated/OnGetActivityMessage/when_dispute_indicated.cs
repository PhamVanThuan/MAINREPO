using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Dispute_Indicated.OnGetActivityMessage
{
    [Subject("Activity => Dispute_Indicated => OnGetActivityMessage")] // AutoGenerated
    internal class when_dispute_indicated : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Dispute_Indicated(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}