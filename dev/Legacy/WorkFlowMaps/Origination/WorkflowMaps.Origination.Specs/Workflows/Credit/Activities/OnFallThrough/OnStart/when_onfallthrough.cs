using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.OnFallThrough.OnStart
{
    [Subject("Activity => OnFallThrough => OnStart")] // AutoGenerated
    internal class when_onfallthrough : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_OnFallThrough(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}