using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Decline___Offer.OnGetStageTransition
{
    [Subject("Activity => Decline___Offer => OnGetStageTransition")] // AutoGenerated
    internal class when_decline___offer : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Decline___Offer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}