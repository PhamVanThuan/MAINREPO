using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Dispute_Indicated.OnComplete
{
    [Subject("Activity => Dispute_Indicated => OnComplete")] // AutoGenerated
    internal class when_dispute_indicated : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Dispute_Indicated(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_set_the_action_source_data_property = () =>
        {
            workflowData.ActionSource.ShouldBeTheSameAs("Dispute Indicated");
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}