using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Pricing_Changes.OnComplete
{
    [Subject("Activity => Pricing_Changes => OnComplete")] // AutoGenerated
    internal class when_pricing_changes : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Pricing_Changes(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}