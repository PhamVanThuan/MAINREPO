using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Approve_with_Pricing_Changes.OnGetActivityMessage
{
    [Subject("Activity => Approve_with_Pricing_Changes => OnGetActivityMessage")] // AutoGenerated
    internal class when_approve_with_pricing_changes : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Approve_with_Pricing_Changes(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}