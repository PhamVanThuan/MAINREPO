using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Request_Further_Info.OnComplete
{
    [Subject("Activity => Request_Further_Info => OnComplete")] // AutoGenerated
    internal class when_request_further_info : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Request_Further_Info(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_set_the_action_source_data_property = () =>
        {
            workflowData.ActionSource.ShouldBeTheSameAs("Request Further Info");
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}