using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Review_not_Required.OnComplete
{
    [Subject("Activity => Review_not_Required => OnComplete")] // AutoGenerated
    internal class when_review_not_required : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Review_not_Required(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}