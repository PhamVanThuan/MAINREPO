using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Resubmission.OnComplete
{
    [Subject("Activity => Resubmission => OnComplete")] // AutoGenerated
    internal class when_resubmission : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Resubmission(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}