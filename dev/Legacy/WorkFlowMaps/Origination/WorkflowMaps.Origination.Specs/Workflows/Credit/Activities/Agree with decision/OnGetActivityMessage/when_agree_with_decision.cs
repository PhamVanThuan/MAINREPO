using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Agree_with_decision.OnGetActivityMessage
{
    [Subject("Activity => Agree_with_decision => OnGetActivityMessage")] // AutoGenerated
    internal class when_agree_with_decision : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Agree_with_decision(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}