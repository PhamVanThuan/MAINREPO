using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.IsResubReturn.OnComplete
{
    [Subject("Activity => IsResubReturn => OnComplete")] // AutoGenerated
    internal class when_isresubreturn : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_IsResubReturn(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}