using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Decline_Application.OnComplete
{
    [Subject("Activity => Decline_Application => OnComplete")] // AutoGenerated
    internal class when_decline_application : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Decline_Application(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_set_action_source_data_property = () =>
        {
            workflowData.ActionSource.ShouldBeTheSameAs("Decline Application");
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}