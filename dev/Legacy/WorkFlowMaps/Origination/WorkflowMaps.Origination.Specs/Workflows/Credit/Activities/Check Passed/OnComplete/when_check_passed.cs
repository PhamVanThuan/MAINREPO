using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Check_Passed.OnComplete
{
    [Subject("Activity => Check_Passed => OnComplete")] // AutoGenerated
    internal class when_check_passed : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Check_Passed(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}