using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Resub.OnComplete
{
    [Subject("Activity => Resub => OnComplete")] // AutoGenerated
    internal class when_resub : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Resub(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}