using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Feedback_on_Override.OnComplete
{
    [Subject("Activity => Feedback_on_Override => OnComplete")] // AutoGenerated
    internal class when_feedback_on_override : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Feedback_on_Override(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}