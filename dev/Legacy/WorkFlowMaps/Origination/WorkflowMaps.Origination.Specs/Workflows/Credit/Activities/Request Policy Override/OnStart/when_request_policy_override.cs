using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Request_Policy_Override.OnStart
{
    [Subject("Activity => Request_Policy_Override => OnStart")] // AutoGenerated
    internal class when_request_policy_override : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Request_Policy_Override(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}