using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Dispute.OnGetStageTransition
{
    [Subject("Activity => Dispute => OnGetStageTransition")] // AutoGenerated
    internal class when_dispute : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Dispute(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}