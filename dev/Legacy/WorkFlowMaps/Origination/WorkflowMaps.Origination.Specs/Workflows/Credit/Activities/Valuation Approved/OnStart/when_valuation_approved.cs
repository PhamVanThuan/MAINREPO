using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Valuation_Approved.OnStart
{
    [Subject("Activity => Valuation_Approved => OnStart")] // AutoGenerated
    internal class when_valuation_approved : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Valuation_Approved(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}