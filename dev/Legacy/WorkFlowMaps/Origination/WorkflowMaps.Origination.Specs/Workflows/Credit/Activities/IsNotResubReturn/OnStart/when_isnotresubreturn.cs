using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.IsNotResubReturn.OnStart
{
    [Subject("Activity => IsNotResubReturn => OnStart")] // AutoGenerated
    internal class when_isnotresubreturn : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_IsNotResubReturn(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}