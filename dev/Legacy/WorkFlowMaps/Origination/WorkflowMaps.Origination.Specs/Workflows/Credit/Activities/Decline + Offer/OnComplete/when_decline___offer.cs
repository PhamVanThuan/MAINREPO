using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Decline___Offer.OnComplete
{
    [Subject("Activity => Decline___Offer => OnComplete")] // AutoGenerated
    internal class when_decline___offer : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Decline___Offer(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}