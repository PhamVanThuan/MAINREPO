using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Assign_Underwriter.OnStart
{
    [Subject("Activity => Assign_Underwriter => OnStart")] // AutoGenerated
    internal class when_assign_underwriter : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Assign_Underwriter(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}