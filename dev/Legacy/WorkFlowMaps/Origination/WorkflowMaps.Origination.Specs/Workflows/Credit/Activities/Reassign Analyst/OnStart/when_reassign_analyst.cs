using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Reassign_Analyst.OnStart
{
    [Subject("Activity => Reassign_Analyst => OnStart")] // AutoGenerated
    internal class when_reassign_analyst : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Reassign_Analyst(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}