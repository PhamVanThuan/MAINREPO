using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Resubmission.OnGetStageTransition
{
    [Subject("Activity => Resubmission => OnGetStageTransition")] // AutoGenerated
    internal class when_resubmission : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Resubmission(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}