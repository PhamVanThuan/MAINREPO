using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Check_Passed.OnGetStageTransition
{
    [Subject("Activity => Check_Passed => OnGetStageTransition")] // AutoGenerated
    internal class when_check_passed : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Check_Passed(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}