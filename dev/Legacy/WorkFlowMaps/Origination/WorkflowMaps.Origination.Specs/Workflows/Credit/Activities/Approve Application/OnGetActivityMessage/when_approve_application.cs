using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Approve_Application.OnGetActivityMessage
{
    [Subject("Activity => Approve_Application => OnGetActivityMessage")] // AutoGenerated
    internal class when_approve_application : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Approve_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}