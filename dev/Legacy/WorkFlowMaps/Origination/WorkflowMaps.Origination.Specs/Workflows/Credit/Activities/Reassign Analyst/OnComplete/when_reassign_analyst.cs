using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Reassign_Analyst.OnComplete
{
    [Subject("Activity => Reassign_Analyst => OnComplete")] // AutoGenerated
    internal class when_reassign_analyst : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Reassign_Analyst(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}