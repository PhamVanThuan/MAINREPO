using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.DoAppManCreate.OnGetStageTransition
{
    [Subject("Activity => DoAppManCreate => OnGetStageTransition")] // AutoGenerated
    internal class when_doappmancreate : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_DoAppManCreate(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}