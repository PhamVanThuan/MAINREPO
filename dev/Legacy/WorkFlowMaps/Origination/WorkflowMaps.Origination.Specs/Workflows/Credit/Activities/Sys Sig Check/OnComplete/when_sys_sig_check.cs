using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Sys_Sig_Check.OnComplete
{
    [Subject("Activity => Sys_Sig_Check => OnComplete")] // AutoGenerated
    internal class when_sys_sig_check : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Sys_Sig_Check(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}