using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Agree_with_decision.OnComplete
{
    [Subject("Activity => Agree_with_decision => OnComplete")] // AutoGenerated
    internal class when_agree_with_decision : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Agree_with_decision(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}