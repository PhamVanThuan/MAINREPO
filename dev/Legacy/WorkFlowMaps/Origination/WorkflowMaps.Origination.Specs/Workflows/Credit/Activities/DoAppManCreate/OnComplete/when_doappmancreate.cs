using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.DoAppManCreate.OnComplete
{
    [Subject("Activity => DoAppManCreate => OnComplete")] // AutoGenerated
    internal class when_doappmancreate : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_DoAppManCreate(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}