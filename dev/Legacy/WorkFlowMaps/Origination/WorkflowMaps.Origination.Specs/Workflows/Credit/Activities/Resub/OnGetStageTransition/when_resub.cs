using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Resub.OnGetStageTransition
{
    [Subject("Activity => Resub => OnGetStageTransition")] // AutoGenerated
    internal class when_resub : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Resub(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}