using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Feedback_on_Override.OnStart
{
    [Subject("Activity => Feedback_on_Override => OnStart")] // AutoGenerated
    internal class when_feedback_on_override : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Feedback_on_Override(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}