using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Review_not_Required.OnGetStageTransition
{
    [Subject("Activity => Review_not_Required => OnGetStageTransition")] // AutoGenerated
    internal class when_review_not_required : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Review_not_Required(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}