using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Agree_with_decision.OnStart
{
    [Subject("Activity => Agree_with_decision => OnStart")] // AutoGenerated
    internal class when_agree_with_decision : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Agree_with_decision(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}