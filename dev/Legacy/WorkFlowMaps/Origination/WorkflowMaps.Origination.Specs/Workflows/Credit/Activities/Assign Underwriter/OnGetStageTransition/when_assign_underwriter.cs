using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Assign_Underwriter.OnGetStageTransition
{
    [Subject("Activity => Assign_Underwriter => OnGetStageTransition")] // AutoGenerated
    internal class when_assign_underwriter : WorkflowSpecCredit
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Assign_Underwriter(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}