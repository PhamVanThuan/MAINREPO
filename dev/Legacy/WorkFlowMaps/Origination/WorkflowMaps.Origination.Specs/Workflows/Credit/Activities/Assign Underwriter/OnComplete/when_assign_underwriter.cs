using Machine.Specifications;

namespace WorkflowMaps.Credit.Specs.Activities.Assign_Underwriter.OnComplete
{
    [Subject("Activity => Assign_Underwriter => OnComplete")] // AutoGenerated
    internal class when_assign_underwriter : WorkflowSpecCredit
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Assign_Underwriter(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}