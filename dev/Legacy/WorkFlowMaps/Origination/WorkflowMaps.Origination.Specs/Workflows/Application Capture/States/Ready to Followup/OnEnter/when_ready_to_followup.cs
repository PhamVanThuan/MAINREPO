using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.Ready_to_Followup.OnEnter
{
    [Subject("State => Ready_to_Followup => OnEnter")] // AutoGenerated
    internal class when_ready_to_followup : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Ready_to_Followup(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}