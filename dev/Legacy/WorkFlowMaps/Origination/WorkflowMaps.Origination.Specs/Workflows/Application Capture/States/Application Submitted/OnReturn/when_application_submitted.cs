using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.Application_Submitted.OnReturn
{
    [Subject("State => Application_Submitted => OnReturn")] // AutoGenerated
    internal class when_application_submitted : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnReturn_Application_Submitted(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}