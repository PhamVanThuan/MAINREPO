using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.Ready_to_Followup.OnExit
{
    [Subject("State => Ready_to_Followup => OnExit")] // AutoGenerated
    internal class when_ready_to_followup : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Ready_to_Followup(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}