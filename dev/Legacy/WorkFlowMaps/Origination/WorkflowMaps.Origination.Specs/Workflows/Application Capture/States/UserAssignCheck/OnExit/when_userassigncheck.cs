using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.UserAssignCheck.OnExit
{
    [Subject("State => UserAssignCheck => OnExit")] // AutoGenerated
    internal class when_userassigncheck : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_UserAssignCheck(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}