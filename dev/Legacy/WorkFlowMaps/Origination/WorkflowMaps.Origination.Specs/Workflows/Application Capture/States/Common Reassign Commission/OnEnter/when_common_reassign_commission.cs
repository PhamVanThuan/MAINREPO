using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.Common_Reassign_Commission.OnEnter
{
    [Subject("State => Common_Reassign_Commission => OnEnter")] // AutoGenerated
    internal class when_common_reassign_commission : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Common_Reassign_Commission(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}