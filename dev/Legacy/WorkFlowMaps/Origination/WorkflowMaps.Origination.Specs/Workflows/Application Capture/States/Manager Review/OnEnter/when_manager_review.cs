using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.Manager_Review.OnEnter
{
    [Subject("State => Manager_Review => OnEnter")] // AutoGenerated
    internal class when_manager_review : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Manager_Review(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}