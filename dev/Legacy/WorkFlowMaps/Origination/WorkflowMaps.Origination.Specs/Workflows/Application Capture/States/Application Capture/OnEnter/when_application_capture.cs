using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.Application_Capture.OnEnter
{
    [Subject("State => Application_Capture => OnEnter")] // AutoGenerated
    internal class when_application_capture : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Application_Capture(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}