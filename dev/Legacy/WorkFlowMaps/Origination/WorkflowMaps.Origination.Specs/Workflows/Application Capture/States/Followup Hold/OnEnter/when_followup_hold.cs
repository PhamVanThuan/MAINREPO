using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.Followup_Hold.OnEnter
{
    [Subject("State => Followup_Hold => OnEnter")] // AutoGenerated
    internal class when_followup_hold : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Followup_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}