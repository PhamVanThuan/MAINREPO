using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.Application_Submitted.OnExit
{
    [Subject("State => Application_Submitted => OnExit")] // AutoGenerated
    internal class when_application_submitted : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Application_Submitted(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}