using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.States.Common_Assign_Admin.OnExit
{
    [Subject("State => Common_Assign_Admin => OnExit")] // AutoGenerated
    internal class when_common_assign_admin : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Common_Assign_Admin(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}