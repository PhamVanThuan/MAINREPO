using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Contact_with_Client.OnStart
{
    [Subject("Activity => Contact_with_Client => OnStart")] // AutoGenerated
    internal class when_contact_with_client : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Contact_with_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}