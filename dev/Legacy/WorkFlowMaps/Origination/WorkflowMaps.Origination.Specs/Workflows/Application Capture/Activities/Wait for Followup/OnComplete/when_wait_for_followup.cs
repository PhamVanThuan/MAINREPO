using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Wait_for_Followup.OnComplete
{
    [Subject("Activity => Wait_for_Followup => OnComplete")] // AutoGenerated
    internal class when_wait_for_followup : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Wait_for_Followup(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}