using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.RetryAssignTeleConsultant.OnGetActivityMessage
{
    [Subject("Activity => RetryAssignTeleConsultant => OnGetActivityMessage")] // AutoGenerated
    internal class when_retryassignteleconsultant : WorkflowSpecApplicationCapture
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetActivityMessage_RetryAssignTeleConsultant(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}