using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.RetryInternetCreate.OnStart
{
    [Subject("Activity => RetryInternetCreate => OnStart")] // AutoGenerated
    internal class when_retryinternetcreate : WorkflowSpecApplicationCapture
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_RetryInternetCreate(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}