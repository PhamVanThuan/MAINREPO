using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Assign_EstateAgent_Lead.OnStart
{
    [Subject("Activity => Assign_EstateAgent_Lead => OnStart")] // AutoGenerated
    internal class when_assign_estateagent_lead : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Assign_EstateAgent_Lead(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}