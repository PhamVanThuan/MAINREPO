using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Contact_with_Client.OnGetActivityMessage
{
    [Subject("Activity => Contact_with_Client => OnGetActivityMessage")] // AutoGenerated
    internal class when_contact_with_client : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Contact_with_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}