using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Reactivate__App_Not_Accepted.OnStart
{
    [Subject("Activity => Reactivate__App_Not_Accepted => OnStart")] // AutoGenerated
    internal class when_reactivate__app_not_accepted : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Reactivate__App_Not_Accepted(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}