using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.ClearRuleCache.OnGetActivityMessage
{
    [Subject("Activity => ClearRuleCache => OnGetActivityMessage")] // AutoGenerated
    internal class when_clearrulecache : WorkflowSpecApplicationCapture
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetActivityMessage_ClearRuleCache(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}