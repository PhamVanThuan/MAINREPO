using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Update_Loan_Details.OnStart
{
    [Subject("Activity => Update_Loan_Details => OnStart")] // AutoGenerated
    internal class when_update_loan_details : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Update_Loan_Details(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}