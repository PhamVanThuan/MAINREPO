using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.ConsultantNotAssigned.OnGetStageTransition
{
    [Subject("Activity => ConsultantNotAssigned => OnGetStageTransition")] // AutoGenerated
    internal class when_consultantnotassigned : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_ConsultantNotAssigned(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}