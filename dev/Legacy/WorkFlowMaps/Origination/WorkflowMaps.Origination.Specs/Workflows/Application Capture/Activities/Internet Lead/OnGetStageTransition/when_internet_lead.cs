using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Internet_Lead.OnGetStageTransition
{
    [Subject("Activity => Internet_Lead => OnGetStageTransition")] // AutoGenerated
    internal class when_internet_lead : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Internet_Lead(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}