using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Update_Followup.OnStart
{
    [Subject("Activity => Update_Followup => OnStart")] // AutoGenerated
    internal class when_update_followup : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Update_Followup(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}