using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.ConsultantAssigned.OnComplete
{
    [Subject("Activity => ConsultantAssigned => OnComplete")] // AutoGenerated
    internal class when_consultantassigned : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_ConsultantAssigned(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}