using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.EXTRefresh_Timer.OnComplete
{
    [Subject("Activity => EXTRefresh_Timer => OnComplete")] // AutoGenerated
    internal class when_extrefresh_timer : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_EXTRefresh_Timer(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}