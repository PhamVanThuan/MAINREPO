using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.ConsultantAssigned.OnGetStageTransition
{
    [Subject("Activity => ConsultantAssigned => OnGetStageTransition")] // AutoGenerated
    internal class when_consultantassigned : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_ConsultantAssigned(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}