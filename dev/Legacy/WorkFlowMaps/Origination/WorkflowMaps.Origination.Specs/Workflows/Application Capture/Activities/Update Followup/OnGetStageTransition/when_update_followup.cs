using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Update_Followup.OnGetStageTransition
{
    [Subject("Activity => Update_Followup => OnGetStageTransition")] // AutoGenerated
    internal class when_update_followup : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Update_Followup(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}