using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Process_Internet_Application.OnGetActivityMessage
{
    [Subject("Activity => Process_Internet_Application => OnGetActivityMessage")] // AutoGenerated
    internal class when_process_internet_application : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Process_Internet_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}