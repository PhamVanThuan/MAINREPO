using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Internet_Lead.OnStart
{
    [Subject("Activity => Internet_Lead => OnStart")] // AutoGenerated
    internal class when_internet_lead : WorkflowSpecApplicationCapture
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_Internet_Lead(messages, workflowData, instanceData, paramsData);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}