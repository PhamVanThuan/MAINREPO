using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.ConsultantNotAssigned.OnComplete
{
    [Subject("Activity => ConsultantNotAssigned => OnComplete")] // AutoGenerated
    internal class when_consultantnotassigned : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_ConsultantNotAssigned(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}