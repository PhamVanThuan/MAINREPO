using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.RetryInternetCreate.OnGetActivityMessage
{
    [Subject("Activity => RetryInternetCreate => OnGetActivityMessage")] // AutoGenerated
    internal class when_retryinternetcreate : WorkflowSpecApplicationCapture
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetActivityMessage_RetryInternetCreate(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}