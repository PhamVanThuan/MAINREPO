using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Update_Loan_Details.OnGetActivityMessage
{
    [Subject("Activity => Update_Loan_Details => OnGetActivityMessage")] // AutoGenerated
    internal class when_update_loan_details : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Update_Loan_Details(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}