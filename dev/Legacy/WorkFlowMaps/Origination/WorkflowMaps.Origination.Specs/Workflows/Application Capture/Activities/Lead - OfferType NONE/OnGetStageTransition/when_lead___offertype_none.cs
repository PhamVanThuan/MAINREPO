using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Lead___OfferType_NONE.OnGetStageTransition
{
    [Subject("Activity => Lead___OfferType_NONE => OnGetStageTransition")] // AutoGenerated
    internal class when_lead___offertype_none : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Lead___OfferType_NONE(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}