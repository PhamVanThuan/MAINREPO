using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Submit_Application.OnGetActivityMessage
{
    [Subject("Activity => Submit_Application => OnGetActivityMessage")] // AutoGenerated
    internal class when_submit_application : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Submit_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}