using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Estate_Agent_Assignment.OnGetActivityMessage
{
    [Subject("Activity => Estate_Agent_Assignment => OnGetActivityMessage")] // AutoGenerated
    internal class when_estate_agent_assignment : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Estate_Agent_Assignment(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}