using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.EXT_Create_Net_Lead.OnStart
{
    [Subject("Activity => EXT_Create_Net_Lead => OnStart")] // AutoGenerated
    internal class when_ext_create_net_lead : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_EXT_Create_Net_Lead(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}