using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Manager_Reassign.OnGetStageTransition
{
    [Subject("Activity => Manager_Reassign => OnGetStageTransition")] // AutoGenerated
    internal class when_manager_reassign : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Manager_Reassign(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}