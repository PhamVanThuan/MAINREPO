using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.MoveToCreate.OnStart
{
    [Subject("Activity => MoveToCreate => OnStart")] // AutoGenerated
    internal class when_movetocreate : WorkflowSpecApplicationCapture
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_MoveToCreate(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}