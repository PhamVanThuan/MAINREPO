using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities._90_day_timer.OnStart
{
    [Subject("Activity => 90_day_timer => OnStart")] // AutoGenerated
    internal class when_90_day_timer : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_90_day_timer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}