using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Assign_EstateAgent.OnComplete
{
    [Subject("Activity => Assign_EstateAgent => OnComplete")] // AutoGenerated
    internal class when_assign_estateagent : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Assign_EstateAgent(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}