using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Continue_with_Application.OnGetActivityMessage
{
    [Subject("Activity => Continue_with_Application => OnGetActivityMessage")] // AutoGenerated
    internal class when_continue_with_application : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Continue_with_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}