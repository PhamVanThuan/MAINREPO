using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Wait_for_Followup.OnGetStageTransition
{
    [Subject("Activity => Wait_for_Followup => OnGetStageTransition")] // AutoGenerated
    internal class when_wait_for_followup : WorkflowSpecApplicationCapture
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Wait_for_Followup(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}