using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Refresh_Application_Timeout.OnStart
{
    [Subject("Activity => Refresh_Application_Timeout => OnStart")] // AutoGenerated
    internal class when_refresh_application_timeout : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Refresh_Application_Timeout(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}