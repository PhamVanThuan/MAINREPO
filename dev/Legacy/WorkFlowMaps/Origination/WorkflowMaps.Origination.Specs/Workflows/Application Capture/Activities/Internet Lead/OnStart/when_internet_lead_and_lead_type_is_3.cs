using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Internet_Lead.OnStart
{
    [Subject("Activity => Internet_Lead => OnStart")] // AutoGenerated
    internal class when_internet_lead_and_lead_type_is_3 : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
            workflowData.LeadType = 3;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Internet_Lead(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}