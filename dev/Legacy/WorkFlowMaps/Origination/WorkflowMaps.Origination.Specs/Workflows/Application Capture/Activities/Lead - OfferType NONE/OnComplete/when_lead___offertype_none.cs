using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Lead___OfferType_NONE.OnComplete
{
    [Subject("Activity => Lead___OfferType_NONE => OnComplete")] // AutoGenerated
    internal class when_lead___offertype_none : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Lead___OfferType_NONE(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}