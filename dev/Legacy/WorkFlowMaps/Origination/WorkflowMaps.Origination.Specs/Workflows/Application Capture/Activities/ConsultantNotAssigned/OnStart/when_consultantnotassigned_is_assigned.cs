using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.ConsultantNotAssigned.OnStart
{
    [Subject("Activity => ConsultantNotAssigned => OnStart")] // AutoGenerated
    internal class when_consultantnotassigned_is_assigned : WorkflowSpecApplicationCapture
    {
        private static bool result;
        private static string expectedAduserName;

        private Establish context = () =>
        {
            result = false;
            expectedAduserName = @"SAHL\YolandaG";
            workflowData.CaseOwnerName = expectedAduserName;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_ConsultantNotAssigned(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_false = () =>
        {
            result.ShouldBeFalse();
        };
    }
}