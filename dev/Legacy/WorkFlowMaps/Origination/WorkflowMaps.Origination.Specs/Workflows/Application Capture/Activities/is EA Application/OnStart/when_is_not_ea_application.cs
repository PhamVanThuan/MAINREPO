using Machine.Specifications;
using WorkflowMaps.ApplicationCapture.Specs;

namespace WorkflowMaps.Origination.Specs.Workflows.Application_Capture.Activities.is_EA_Application.OnStart
{
    [Subject("Activity => is_EA_Application => OnStart")] // AutoGenerated
    internal class when_is_not_ea_application : WorkflowSpecApplicationCapture
    {
        private static bool result;

        private Establish context = () =>
        {
            result = true;
            workflowData.isEstateAgentApplication = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_is_EA_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_false = () =>
        {
            result.ShouldBeFalse();
        };
    }
}