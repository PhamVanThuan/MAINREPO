using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Internet_Application.OnStart
{
    [Subject("Activity => Internet_Application => OnStart")] // AutoGenerated
    internal class when_internet_application : WorkflowSpecApplicationCapture
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_Internet_Application(messages, workflowData, instanceData, paramsData);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}