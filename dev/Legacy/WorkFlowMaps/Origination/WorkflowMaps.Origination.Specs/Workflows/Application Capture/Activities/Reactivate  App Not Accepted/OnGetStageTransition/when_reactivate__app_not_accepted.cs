using Machine.Specifications;

namespace WorkflowMaps.ApplicationCapture.Specs.Activities.Reactivate__App_Not_Accepted.OnGetStageTransition
{
    [Subject("Activity => Reactivate__App_Not_Accepted => OnGetStageTransition")] // AutoGenerated
    internal class when_reactivate__app_not_accepted : WorkflowSpecApplicationCapture
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_Reactivate__App_Not_Accepted(messages, workflowData, instanceData, paramsData);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}