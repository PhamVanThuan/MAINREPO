using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Archive_NTU.OnStart
{
    [Subject("Activity => Archive_NTU => OnStart")] // AutoGenerated
    internal class when_archive_ntu : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Archive_NTU(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}