using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Archive_NTU.OnComplete
{
    [Subject("Activity => Archive_NTU => OnComplete")] // AutoGenerated
    internal class when_archive_ntu : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Archive_NTU(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}