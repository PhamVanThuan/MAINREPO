using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Accept_RPAR.OnStart
{
    [Subject("Activity => Accept_RPAR => OnStart")] // AutoGenerated
    internal class when_accept_rpar : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Accept_RPAR(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}