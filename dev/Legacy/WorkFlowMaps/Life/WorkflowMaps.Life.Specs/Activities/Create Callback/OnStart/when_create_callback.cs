using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Create_Callback.OnStart
{
    [Subject("Activity => Create_Callback => OnStart")] // AutoGenerated
    internal class when_create_callback : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Create_Callback(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}