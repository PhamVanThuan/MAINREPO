using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Confirmation_Required.OnGetStageTransition
{
    [Subject("Activity => Confirmation_Required => OnGetStageTransition")] // AutoGenerated
    internal class when_confirmation_required : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Confirmation_Required(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}