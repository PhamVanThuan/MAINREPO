using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities._45_Day_Timeout.OnComplete
{
    [Subject("Activity => 45_Day_Timeout => OnComplete")] // AutoGenerated
    internal class when_45_day_timeout : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_45_Day_Timeout(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}