using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities._45_Day_Timeout.OnStart
{
    [Subject("Activity => 45_Day_Timeout => OnStart")] // AutoGenerated
    internal class when_45_day_timeout : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_45_Day_Timeout(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}