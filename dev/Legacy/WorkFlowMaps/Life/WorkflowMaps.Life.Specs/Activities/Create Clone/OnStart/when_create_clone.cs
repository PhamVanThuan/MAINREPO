using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Create_Clone.OnStart
{
    [Subject("Activity => Create_Clone => OnStart")] // AutoGenerated
    internal class when_create_clone : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Create_Clone(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}