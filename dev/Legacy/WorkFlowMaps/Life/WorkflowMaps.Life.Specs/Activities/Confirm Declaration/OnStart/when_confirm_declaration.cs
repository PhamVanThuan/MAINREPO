using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Confirm_Declaration.OnStart
{
    [Subject("Activity => Confirm_Declaration => OnStart")] // AutoGenerated
    internal class when_confirm_declaration : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Confirm_Declaration(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}