using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Awaiting_Confirmation_Timout.OnStart
{
    [Subject("Activity => Awaiting_Confirmation_Timout => OnStart")] // AutoGenerated
    internal class when_awaiting_confirmation_timout : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Awaiting_Confirmation_Timout(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}