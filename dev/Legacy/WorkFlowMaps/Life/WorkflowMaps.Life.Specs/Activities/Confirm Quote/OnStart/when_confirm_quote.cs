using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Confirm_Quote.OnStart
{
    [Subject("Activity => Confirm_Quote => OnStart")] // AutoGenerated
    internal class when_confirm_quote : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Confirm_Quote(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}