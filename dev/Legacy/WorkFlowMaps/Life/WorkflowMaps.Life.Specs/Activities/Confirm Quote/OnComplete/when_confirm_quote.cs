using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Confirm_Quote.OnComplete
{
    [Subject("Activity => Confirm_Quote => OnComplete")] // AutoGenerated
    internal class when_confirm_quote : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Confirm_Quote(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}