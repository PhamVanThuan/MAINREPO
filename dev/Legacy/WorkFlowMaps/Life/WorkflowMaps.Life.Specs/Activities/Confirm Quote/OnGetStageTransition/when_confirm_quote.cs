using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Confirm_Quote.OnGetStageTransition
{
    [Subject("Activity => Confirm_Quote => OnGetStageTransition")] // AutoGenerated
    internal class when_confirm_quote : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Confirm_Quote(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}