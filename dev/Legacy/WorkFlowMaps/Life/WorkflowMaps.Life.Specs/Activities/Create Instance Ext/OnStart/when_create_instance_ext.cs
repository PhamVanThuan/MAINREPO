using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Create_Instance_Ext.OnStart
{
    [Subject("Activity => Create_Instance_Ext => OnStart")] // AutoGenerated
    internal class when_create_instance_ext : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Create_Instance_Ext(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}