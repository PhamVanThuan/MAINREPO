using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Send_Quote.OnGetStageTransition
{
    [Subject("Activity => Send_Quote => OnGetStageTransition")] // AutoGenerated
    internal class when_send_quote : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Send_Quote(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}