using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Send_Quote.OnStart
{
    [Subject("Activity => Send_Quote => OnStart")] // AutoGenerated
    internal class when_send_quote : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Send_Quote(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}