using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Reactivate_NTUd_Policy.OnStart
{
    [Subject("Activity => Reactivate_NTUd_Policy => OnStart")] // AutoGenerated
    internal class when_reactivate_ntud_policy : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Reactivate_NTUd_Policy(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}