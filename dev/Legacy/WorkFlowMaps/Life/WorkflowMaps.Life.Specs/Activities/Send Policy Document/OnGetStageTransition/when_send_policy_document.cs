using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Send_Policy_Document.OnGetStageTransition
{
    [Subject("Activity => Send_Policy_Document => OnGetStageTransition")] // AutoGenerated
    internal class when_send_policy_document : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Send_Policy_Document(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}