using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.isAccidentalCover.OnComplete
{
    [Subject("Activity => isAccidentalCover => OnComplete")] // AutoGenerated
    internal class when_isaccidentalcover : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_isAccidentalCover(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}