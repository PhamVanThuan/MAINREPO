using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.isAccidentalCover.OnGetStageTransition
{
    [Subject("Activity => isAccidentalCover => OnGetStageTransition")] // AutoGenerated
    internal class when_isaccidentalcover : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_isAccidentalCover(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}