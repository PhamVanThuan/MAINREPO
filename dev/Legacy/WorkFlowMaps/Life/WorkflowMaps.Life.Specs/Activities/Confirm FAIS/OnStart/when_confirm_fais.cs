using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Confirm_FAIS.OnStart
{
    [Subject("Activity => Confirm_FAIS => OnStart")] // AutoGenerated
    internal class when_confirm_fais : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Confirm_FAIS(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}