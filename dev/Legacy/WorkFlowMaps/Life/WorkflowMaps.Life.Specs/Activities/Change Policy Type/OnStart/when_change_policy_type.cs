using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Change_Policy_Type.OnStart
{
    [Subject("Activity => Change_Policy_Type => OnStart")] // AutoGenerated
    internal class when_change_policy_type : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Change_Policy_Type(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}