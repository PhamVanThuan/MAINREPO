using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Change_Policy_Type.OnGetActivityMessage
{
    [Subject("Activity => Change_Policy_Type => OnGetActivityMessage")] // AutoGenerated
    internal class when_change_policy_type : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Change_Policy_Type(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}