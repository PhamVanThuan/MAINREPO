using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Confirm_Exclusions.OnStart
{
    [Subject("Activity => Confirm_Exclusions => OnStart")] // AutoGenerated
    internal class when_confirm_exclusions : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Confirm_Exclusions(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}