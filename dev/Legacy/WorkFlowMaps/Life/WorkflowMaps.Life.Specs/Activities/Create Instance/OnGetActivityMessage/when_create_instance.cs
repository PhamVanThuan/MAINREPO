using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Create_Instance_Ext.OnGetActivityMessage
{
    [Subject("Activity => Create_Instance_Ext => OnGetActivityMessage")] // AutoGenerated
    internal class when_create_instance_ext : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Create_Instance(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}