using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Continue_with_Sale.OnStart
{
    [Subject("Activity => Continue_with_Sale => OnStart")] // AutoGenerated
    internal class when_continue_with_sale : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Continue_with_Sale(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}