using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Accept_Exclusions.OnGetStageTransition
{
    [Subject("Activity => Accept_Exclusions => OnGetStageTransition")] // AutoGenerated
    internal class when_accept_exclusions : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Accept_Exclusions(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}