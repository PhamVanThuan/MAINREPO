using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Accept_Declaration.OnStart
{
    [Subject("Activity => Accept_Declaration => OnStart")] // AutoGenerated
    internal class when_accept_declaration : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Accept_Declaration(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}