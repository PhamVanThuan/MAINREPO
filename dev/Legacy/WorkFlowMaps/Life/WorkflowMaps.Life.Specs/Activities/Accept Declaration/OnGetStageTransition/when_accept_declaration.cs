using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Accept_Declaration.OnGetStageTransition
{
    [Subject("Activity => Accept_Declaration => OnGetStageTransition")] // AutoGenerated
    internal class when_accept_declaration : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Accept_Declaration(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}