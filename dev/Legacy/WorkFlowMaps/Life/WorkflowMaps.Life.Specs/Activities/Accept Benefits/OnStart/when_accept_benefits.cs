using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Accept_Benefits.OnStart
{
    [Subject("Activity => Accept_Benefits => OnStart")] // AutoGenerated
    internal class when_accept_benefits : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Accept_Benefits(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}