using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Accept_Benefits.OnGetActivityMessage
{
    [Subject("Activity => Accept_Benefits => OnGetActivityMessage")] // AutoGenerated
    internal class when_accept_benefits : WorkflowSpecLife
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Accept_Benefits(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}