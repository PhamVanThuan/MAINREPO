using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Confirm_Details.OnStart
{
    [Subject("Activity => Confirm_Details => OnStart")] // AutoGenerated
    internal class when_confirm_details : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Confirm_Details(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}