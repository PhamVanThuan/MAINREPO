using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Wait_for_Callback.OnComplete
{
    [Subject("Activity => Wait_for_Callback => OnComplete")] // AutoGenerated
    internal class when_wait_for_callback : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Wait_for_Callback(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}