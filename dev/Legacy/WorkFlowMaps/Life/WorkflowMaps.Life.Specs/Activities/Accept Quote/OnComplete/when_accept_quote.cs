using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Accept_Quote.OnComplete
{
    [Subject("Activity => Accept_Quote => OnComplete")] // AutoGenerated
    internal class when_accept_quote : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Accept_Quote(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}