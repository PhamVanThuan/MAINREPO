using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.Activities.Decline_Quote.OnStart
{
    [Subject("Activity => Decline_Quote => OnStart")] // AutoGenerated
    internal class when_decline_quote : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Decline_Quote(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}