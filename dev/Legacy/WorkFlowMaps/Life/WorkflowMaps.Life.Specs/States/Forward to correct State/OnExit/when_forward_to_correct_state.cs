using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Forward_to_correct_State.OnExit
{
    [Subject("State => Forward_to_correct_State => OnExit")] // AutoGenerated
    internal class when_forward_to_correct_state : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Forward_to_correct_State(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}