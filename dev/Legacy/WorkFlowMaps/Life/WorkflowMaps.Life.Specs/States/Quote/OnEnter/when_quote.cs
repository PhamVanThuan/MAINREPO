using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Quote.OnEnter
{
    [Subject("State => Quote => OnEnter")] // AutoGenerated
    internal class when_quote : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Quote(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}