using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.FAIS_Confirmation.OnExit
{
    [Subject("State => FAIS_Confirmation => OnExit")] // AutoGenerated
    internal class when_fais_confirmation : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_FAIS_Confirmation(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}