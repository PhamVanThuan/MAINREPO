using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.FAIS.OnEnter
{
    [Subject("State => FAIS => OnEnter")] // AutoGenerated
    internal class when_fais : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_FAIS(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}