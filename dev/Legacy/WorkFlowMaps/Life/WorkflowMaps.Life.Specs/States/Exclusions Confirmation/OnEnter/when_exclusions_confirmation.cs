using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Exclusions_Confirmation.OnEnter
{
    [Subject("State => Exclusions_Confirmation => OnEnter")] // AutoGenerated
    internal class when_exclusions_confirmation : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Exclusions_Confirmation(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}