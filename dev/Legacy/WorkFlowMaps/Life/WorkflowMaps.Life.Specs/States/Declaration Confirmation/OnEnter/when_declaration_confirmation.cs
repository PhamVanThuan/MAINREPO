using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Declaration_Confirmation.OnEnter
{
    [Subject("State => Declaration_Confirmation => OnEnter")] // AutoGenerated
    internal class when_declaration_confirmation : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Declaration_Confirmation(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}