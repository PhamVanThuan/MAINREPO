using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.LOA.OnEnter
{
    [Subject("State => LOA => OnEnter")] // AutoGenerated
    internal class when_loa : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_LOA(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}