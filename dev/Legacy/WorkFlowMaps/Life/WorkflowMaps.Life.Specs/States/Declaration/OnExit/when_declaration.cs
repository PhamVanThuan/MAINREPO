using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Declaration.OnExit
{
    [Subject("State => Declaration => OnExit")] // AutoGenerated
    internal class when_declaration : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Declaration(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}