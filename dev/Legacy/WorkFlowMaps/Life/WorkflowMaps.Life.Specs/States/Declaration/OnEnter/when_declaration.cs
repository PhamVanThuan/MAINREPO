using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Declaration.OnEnter
{
    [Subject("State => Declaration => OnEnter")] // AutoGenerated
    internal class when_declaration : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Declaration(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}