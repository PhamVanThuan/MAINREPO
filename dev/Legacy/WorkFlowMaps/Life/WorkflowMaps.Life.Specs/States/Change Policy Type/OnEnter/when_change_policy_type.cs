using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Change_Policy_Type.OnEnter
{
    [Subject("State => Change_Policy_Type => OnEnter")] // AutoGenerated
    internal class when_change_policy_type : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Change_Policy_Type(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}