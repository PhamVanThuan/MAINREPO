using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Contact_Client.OnExit
{
    [Subject("State => Contact_Client => OnExit")] // AutoGenerated
    internal class when_contact_client : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Contact_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}