using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Parent_Closed.OnExit
{
    [Subject("State => Parent_Closed => OnExit")] // AutoGenerated
    internal class when_parent_closed : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Parent_Closed(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}