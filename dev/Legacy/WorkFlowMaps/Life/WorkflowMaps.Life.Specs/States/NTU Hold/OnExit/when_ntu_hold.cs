using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.NTU_Hold.OnExit
{
    [Subject("State => NTU_Hold => OnExit")] // AutoGenerated
    internal class when_ntu_hold : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_NTU_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}