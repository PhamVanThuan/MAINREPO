using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.NTU_Hold.OnEnter
{
    [Subject("State => NTU_Hold => OnEnter")] // AutoGenerated
    internal class when_ntu_hold : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_NTU_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}