using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Callback_Hold.OnEnter
{
    [Subject("State => Callback_Hold => OnEnter")] // AutoGenerated
    internal class when_callback_hold : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Callback_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}