using Machine.Specifications;

namespace WorkflowMaps.Life.Specs.States.Callback_Hold.OnExit
{
    [Subject("State => Callback_Hold => OnExit")] // AutoGenerated
    internal class when_callback_hold : WorkflowSpecLife
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Callback_Hold(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}