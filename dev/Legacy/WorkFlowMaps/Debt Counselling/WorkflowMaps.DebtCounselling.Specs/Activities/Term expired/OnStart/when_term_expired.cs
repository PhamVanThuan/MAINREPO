using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Term_expired.OnStart
{
    [Subject("Activity => Term_expired => OnStart")] // AutoGenerated
    internal class when_term_expired : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Term_expired(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}