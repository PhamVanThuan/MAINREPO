using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Update_Case_Details.OnStart
{
    [Subject("Activity => Update_Case_Details => OnStart")] // AutoGenerated
    internal class when_update_case_details : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Update_Case_Details(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}