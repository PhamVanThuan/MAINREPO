using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Update_Case_Details.OnComplete
{
    [Subject("Activity => Update_Case_Details => OnComplete")] // AutoGenerated
    internal class when_update_case_details : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Update_Case_Details(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}