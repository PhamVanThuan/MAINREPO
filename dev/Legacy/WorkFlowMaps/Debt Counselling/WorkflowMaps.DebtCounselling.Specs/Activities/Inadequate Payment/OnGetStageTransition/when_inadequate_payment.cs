using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Inadequate_Payment.OnGetStageTransition
{
    [Subject("Activity => Inadequate_Payment => OnGetStageTransition")] // AutoGenerated
    internal class when_inadequate_payment : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Inadequate_Payment(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}