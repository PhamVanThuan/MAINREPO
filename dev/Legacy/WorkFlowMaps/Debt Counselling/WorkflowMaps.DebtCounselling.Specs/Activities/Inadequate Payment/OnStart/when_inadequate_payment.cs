using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Inadequate_Payment.OnStart
{
    [Subject("Activity => Inadequate_Payment => OnStart")] // AutoGenerated
    internal class when_inadequate_payment : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Inadequate_Payment(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}