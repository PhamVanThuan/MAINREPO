using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Signed_Documents_Received.OnGetStageTransition
{
    [Subject("Activity => Signed_Documents_Received => OnGetStageTransition")] // AutoGenerated
    internal class when_signed_documents_received : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Signed_Documents_Received(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}