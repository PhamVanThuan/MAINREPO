using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Signed_Documents_Received.OnStart
{
    [Subject("Activity => Signed_Documents_Received => OnStart")] // AutoGenerated
    internal class when_signed_documents_received : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Signed_Documents_Received(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}