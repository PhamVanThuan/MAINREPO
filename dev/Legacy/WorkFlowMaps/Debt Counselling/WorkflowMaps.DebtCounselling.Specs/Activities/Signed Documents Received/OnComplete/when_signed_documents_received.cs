using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Signed_Documents_Received.OnComplete
{
    [Subject("Activity => Signed_Documents_Received => OnComplete")] // AutoGenerated
    internal class when_signed_documents_received : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Signed_Documents_Received(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}