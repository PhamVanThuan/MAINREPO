using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Send_Decline_Letter.OnGetStageTransition
{
    [Subject("Activity => Send_Decline_Letter => OnGetStageTransition")] // AutoGenerated
    internal class when_send_decline_letter : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Send_Decline_Letter(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}