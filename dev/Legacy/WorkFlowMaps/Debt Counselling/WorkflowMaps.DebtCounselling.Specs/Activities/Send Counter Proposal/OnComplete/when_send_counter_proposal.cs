using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Send_Counter_Proposal.OnComplete
{
    [Subject("Activity => Send_Counter_Proposal => OnComplete")] // AutoGenerated
    internal class when_send_counter_proposal : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Send_Counter_Proposal(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}