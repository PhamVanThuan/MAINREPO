using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Respond_to_Debt_Counsellor.OnStart
{
    [Subject("Activity => Respond_to_Debt_Counsellor => OnStart")] // AutoGenerated
    internal class when_respond_to_debt_counsellor : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Respond_to_Debt_Counsellor(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}