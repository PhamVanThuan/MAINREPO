using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Payment_in_Order.OnGetStageTransition
{
    [Subject("Activity => Payment_in_Order => OnGetStageTransition")] // AutoGenerated
    internal class when_payment_in_order : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Payment_in_Order(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}