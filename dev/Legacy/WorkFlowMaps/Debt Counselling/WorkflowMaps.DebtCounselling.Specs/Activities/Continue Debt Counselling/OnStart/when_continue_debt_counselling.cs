using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Continue_Debt_Counselling.OnStart
{
    [Subject("Activity => Continue_Debt_Counselling => OnStart")] // AutoGenerated
    internal class when_continue_debt_counselling : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Continue_Debt_Counselling(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}