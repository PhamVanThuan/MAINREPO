using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Continue_Debt_Counselling.OnComplete
{
    [Subject("Activity => Continue_Debt_Counselling => OnComplete")] // AutoGenerated
    internal class when_continue_debt_counselling : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Continue_Debt_Counselling(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}