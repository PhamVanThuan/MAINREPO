using Machine.Specifications;
using X2DomainService.Interface.DebtCounselling;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Cancel_PL_Debt_Counselling.OnStart
{
    [Subject("Activity => Cancel_PL_Debt_Counselling => OnStart")] // AutoGenerated
    internal class when_cancel_pl_debt_counselling_and_rule_pass : WorkflowSpecDebtCounselling
    {
        private static bool result;
        private static IDebtCounselling client;

        private Establish context = () =>
        {
            result = true;
            workflowData.ProductKey = 12;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Cancel_PL_Debt_Counselling(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}