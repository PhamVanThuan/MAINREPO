using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Capture_Recoveries_Proposal.OnStart
{
    [Subject("Activity => Capture_Recoveries_Proposal => OnStart")] // AutoGenerated
    internal class when_capture_recoveries_proposal : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Capture_Recoveries_Proposal(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}