using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Negotiate_Proposal.OnGetStageTransition
{
    [Subject("Activity => Negotiate_Proposal => OnGetStageTransition")] // AutoGenerated
    internal class when_negotiate_proposal : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Negotiate_Proposal(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}