using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities._45_Days.OnComplete
{
    [Subject("Activity => 45_Days => OnComplete")] // AutoGenerated
    internal class when_45_days : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_45_Days(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}