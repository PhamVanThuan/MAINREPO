using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Terminate_Application.OnStart
{
    [Subject("Activity => Terminate_Application => OnStart")] // AutoGenerated
    internal class when_terminate_application : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Terminate_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}