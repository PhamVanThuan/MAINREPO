using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Terminate_Application.OnGetActivityMessage
{
    [Subject("Activity => Terminate_Application => OnGetActivityMessage")] // AutoGenerated
    internal class when_terminate_application : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Terminate_Application(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}