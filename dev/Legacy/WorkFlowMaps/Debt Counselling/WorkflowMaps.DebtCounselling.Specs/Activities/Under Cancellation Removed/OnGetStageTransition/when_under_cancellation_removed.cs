using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Under_Cancellation_Removed.OnGetStageTransition
{
    [Subject("Activity => Under_Cancellation_Removed => OnGetStageTransition")] // AutoGenerated
    internal class when_under_cancellation_removed : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Under_Cancellation_Removed(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}