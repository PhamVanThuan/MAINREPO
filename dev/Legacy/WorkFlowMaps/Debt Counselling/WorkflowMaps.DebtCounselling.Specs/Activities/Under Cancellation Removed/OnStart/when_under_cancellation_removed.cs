using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Under_Cancellation_Removed.OnStart
{
    [Subject("Activity => Under_Cancellation_Removed => OnStart")] // AutoGenerated
    internal class when_under_cancellation_removed : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Under_Cancellation_Removed(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}