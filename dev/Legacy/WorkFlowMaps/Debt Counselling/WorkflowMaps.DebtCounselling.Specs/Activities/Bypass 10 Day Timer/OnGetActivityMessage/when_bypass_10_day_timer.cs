using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Bypass_10_Day_Timer.OnGetActivityMessage
{
    [Subject("Activity => Bypass_10_Day_Timer => OnGetActivityMessage")] // AutoGenerated
    internal class when_bypass_10_day_timer : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Bypass_10_Day_Timer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}