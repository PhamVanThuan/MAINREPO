using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Bypass_10_Day_Timer.OnComplete
{
    [Subject("Activity => Bypass_10_Day_Timer => OnComplete")] // AutoGenerated
    internal class when_bypass_10_day_timer : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Bypass_10_Day_Timer(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}