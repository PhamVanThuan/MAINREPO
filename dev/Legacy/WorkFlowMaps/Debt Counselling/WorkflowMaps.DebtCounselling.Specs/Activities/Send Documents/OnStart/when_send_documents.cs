using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Send_Documents.OnStart
{
    [Subject("Activity => Send_Documents => OnStart")] // AutoGenerated
    internal class when_send_documents : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Send_Documents(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}