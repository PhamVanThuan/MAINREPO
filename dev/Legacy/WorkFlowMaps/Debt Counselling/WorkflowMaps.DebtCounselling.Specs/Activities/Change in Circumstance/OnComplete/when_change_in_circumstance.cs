using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Change_in_Circumstance.OnComplete
{
    [Subject("Activity => Change_in_Circumstance => OnComplete")] // AutoGenerated
    internal class when_change_in_circumstance : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Change_in_Circumstance(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}