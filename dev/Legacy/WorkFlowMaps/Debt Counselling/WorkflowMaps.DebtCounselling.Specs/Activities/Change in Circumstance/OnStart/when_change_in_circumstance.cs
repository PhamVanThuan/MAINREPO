using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Change_in_Circumstance.OnStart
{
    [Subject("Activity => Change_in_Circumstance => OnStart")] // AutoGenerated
    internal class when_change_in_circumstance : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Change_in_Circumstance(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}