using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Court_Date_or_Deposit.OnGetStageTransition
{
    [Subject("Activity => Court_Date_or_Deposit => OnGetStageTransition")] // AutoGenerated
    internal class when_court_date_or_deposit : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Court_Date_or_Deposit(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}