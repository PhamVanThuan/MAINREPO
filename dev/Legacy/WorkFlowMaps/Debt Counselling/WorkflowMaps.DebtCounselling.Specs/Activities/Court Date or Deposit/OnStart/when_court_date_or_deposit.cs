using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Court_Date_or_Deposit.OnStart
{
    [Subject("Activity => Court_Date_or_Deposit => OnStart")] // AutoGenerated
    internal class when_court_date_or_deposit : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Court_Date_or_Deposit(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}