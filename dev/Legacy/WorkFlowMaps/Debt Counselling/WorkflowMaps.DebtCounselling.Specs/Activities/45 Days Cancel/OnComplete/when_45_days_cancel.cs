using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities._45_Days_Cancel.OnComplete
{
    [Subject("Activity => 45_Days_Cancel => OnComplete")] // AutoGenerated
    internal class when_45_days_cancel : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_45_Days_Cancel(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}