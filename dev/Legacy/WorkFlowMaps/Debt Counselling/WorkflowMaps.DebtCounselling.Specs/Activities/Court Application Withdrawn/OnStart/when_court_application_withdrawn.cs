using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Court_Application_Withdrawn.OnStart
{
    [Subject("Activity => Court_Application_Withdrawn => OnStart")] // AutoGenerated
    internal class when_court_application_withdrawn : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Court_Application_Withdrawn(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}