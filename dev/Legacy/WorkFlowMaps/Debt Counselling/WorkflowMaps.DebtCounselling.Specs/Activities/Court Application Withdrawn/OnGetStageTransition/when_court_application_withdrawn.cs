using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Court_Application_Withdrawn.OnGetStageTransition
{
    [Subject("Activity => Court_Application_Withdrawn => OnGetStageTransition")] // AutoGenerated
    internal class when_court_application_withdrawn : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Court_Application_Withdrawn(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}