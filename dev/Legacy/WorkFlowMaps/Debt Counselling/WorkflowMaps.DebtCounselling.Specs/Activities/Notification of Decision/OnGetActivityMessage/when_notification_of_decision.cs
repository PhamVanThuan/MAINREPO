using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Notification_of_Decision.OnGetActivityMessage
{
    [Subject("Activity => Notification_of_Decision => OnGetActivityMessage")] // AutoGenerated
    internal class when_notification_of_decision : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Notification_of_Decision(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}