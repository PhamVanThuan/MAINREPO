using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Notification_of_Decision.OnStart
{
    [Subject("Activity => Notification_of_Decision => OnStart")] // AutoGenerated
    internal class when_notification_of_decision : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Notification_of_Decision(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}