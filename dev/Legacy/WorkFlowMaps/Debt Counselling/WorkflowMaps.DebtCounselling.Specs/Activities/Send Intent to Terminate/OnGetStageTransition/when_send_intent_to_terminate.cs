using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Send_Intent_to_Terminate.OnGetStageTransition
{
    [Subject("Activity => Send_Intent_to_Terminate => OnGetStageTransition")] // AutoGenerated
    internal class when_send_intent_to_terminate : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Send_Intent_to_Terminate(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}