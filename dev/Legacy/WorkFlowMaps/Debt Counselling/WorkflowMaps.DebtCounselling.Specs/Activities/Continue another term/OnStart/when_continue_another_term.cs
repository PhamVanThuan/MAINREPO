using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Continue_another_term.OnStart
{
    [Subject("Activity => Continue_another_term => OnStart")] // AutoGenerated
    internal class when_continue_another_term : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Continue_another_term(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}