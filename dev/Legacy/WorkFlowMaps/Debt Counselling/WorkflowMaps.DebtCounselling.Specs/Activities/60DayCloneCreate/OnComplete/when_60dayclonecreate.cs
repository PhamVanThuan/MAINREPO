using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities._60DayCloneCreate.OnComplete
{
    [Subject("Activity => 60DayCloneCreate => OnComplete")] // AutoGenerated
    internal class when_60dayclonecreate : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_60DayCloneCreate(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}