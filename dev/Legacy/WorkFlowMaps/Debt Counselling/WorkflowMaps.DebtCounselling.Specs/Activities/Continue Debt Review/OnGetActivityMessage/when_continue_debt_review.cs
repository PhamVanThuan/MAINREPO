using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Continue_Debt_Review.OnGetActivityMessage
{
    [Subject("Activity => Continue_Debt_Review => OnGetActivityMessage")] // AutoGenerated
    internal class when_continue_debt_review : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Continue_Debt_Review(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}