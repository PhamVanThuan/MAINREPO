using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Approve_Shortfall.OnGetActivityMessage
{
    [Subject("Activity => Approve_Shortfall => OnGetActivityMessage")] // AutoGenerated
    internal class when_approve_shortfall : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Approve_Shortfall(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}