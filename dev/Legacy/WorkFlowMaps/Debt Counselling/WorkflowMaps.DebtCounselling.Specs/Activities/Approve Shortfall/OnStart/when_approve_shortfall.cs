using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Approve_Shortfall.OnStart
{
    [Subject("Activity => Approve_Shortfall => OnStart")] // AutoGenerated
    internal class when_approve_shortfall : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Approve_Shortfall(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}