using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Create_Debt_Counselling_Case.OnGetStageTransition
{
    [Subject("Activity => Create_Debt_Counselling_Case => OnGetStageTransition")] // AutoGenerated
    internal class when_create_debt_counselling_case : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Create_Debt_Counselling_Case(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}