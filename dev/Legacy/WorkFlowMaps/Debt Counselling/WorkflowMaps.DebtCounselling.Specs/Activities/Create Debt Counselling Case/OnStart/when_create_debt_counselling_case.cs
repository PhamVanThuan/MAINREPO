using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Create_Debt_Counselling_Case.OnStart
{
    [Subject("Activity => Create_Debt_Counselling_Case => OnStart")] // AutoGenerated
    internal class when_create_debt_counselling_case : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Create_Debt_Counselling_Case(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}