using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Court_Details.OnStart
{
    [Subject("Activity => Court_Details => OnStart")] // AutoGenerated
    internal class when_court_details : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Court_Details(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}