using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities._45_day_reminder_sent.OnStart
{
    [Subject("Activity => 45_day_reminder_sent => OnStart")] // AutoGenerated
    internal class when_45_day_reminder_sent : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_45_day_reminder_sent(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}