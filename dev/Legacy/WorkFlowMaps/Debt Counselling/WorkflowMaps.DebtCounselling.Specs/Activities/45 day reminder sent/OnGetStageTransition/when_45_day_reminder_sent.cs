using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities._45_day_reminder_sent.OnGetStageTransition
{
    [Subject("Activity => 45_day_reminder_sent => OnGetStageTransition")] // AutoGenerated
    internal class when_45_day_reminder_sent : WorkflowSpecDebtCounselling
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_45_day_reminder_sent(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}