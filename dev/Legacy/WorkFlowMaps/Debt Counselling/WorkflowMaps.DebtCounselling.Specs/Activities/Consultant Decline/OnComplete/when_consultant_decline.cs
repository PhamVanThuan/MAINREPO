using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.Activities.Consultant_Decline.OnComplete
{
    [Subject("Activity => Consultant_Decline => OnComplete")] // AutoGenerated
    internal class when_consultant_decline : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Consultant_Decline(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}