using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Payment_Review.OnEnter
{
    [Subject("State => Payment_Review => OnEnter")] // AutoGenerated
    internal class when_payment_review : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Payment_Review(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}