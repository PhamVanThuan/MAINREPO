using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Check_No_Date_No_Pay.OnExit
{
    [Subject("State => Check_No_Date_No_Pay => OnExit")] // AutoGenerated
    internal class when_check_no_date_no_pay : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Check_No_Date_No_Pay(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}