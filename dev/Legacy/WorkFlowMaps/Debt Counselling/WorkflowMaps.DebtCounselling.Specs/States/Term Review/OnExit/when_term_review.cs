using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Term_Review.OnExit
{
    [Subject("State => Term_Review => OnExit")] // AutoGenerated
    internal class when_term_review : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Term_Review(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}