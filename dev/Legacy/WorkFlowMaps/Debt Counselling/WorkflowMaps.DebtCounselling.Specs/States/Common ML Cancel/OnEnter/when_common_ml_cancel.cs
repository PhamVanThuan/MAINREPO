using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Common_ML_Cancel.OnEnter
{
    [Subject("State => Common_ML_Cancel => OnEnter")] // AutoGenerated
    internal class when_common_ml_cancel : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Common_ML_Cancel(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}