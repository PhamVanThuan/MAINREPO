using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Common_Exclude_Bond.OnEnter
{
    [Subject("State => Common_Exclude_Bond => OnEnter")] // AutoGenerated
    internal class when_common_exclude_bond : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Common_Exclude_Bond(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}