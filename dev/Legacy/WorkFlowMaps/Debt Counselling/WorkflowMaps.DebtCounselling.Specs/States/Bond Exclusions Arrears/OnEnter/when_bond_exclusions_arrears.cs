using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Bond_Exclusions_Arrears.OnEnter
{
    [Subject("State => Bond_Exclusions_Arrears => OnEnter")] // AutoGenerated
    internal class when_bond_exclusions_arrears : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Bond_Exclusions_Arrears(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}