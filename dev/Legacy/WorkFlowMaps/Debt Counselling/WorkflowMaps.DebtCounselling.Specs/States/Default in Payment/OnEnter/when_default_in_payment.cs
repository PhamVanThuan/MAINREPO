using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Default_in_Payment.OnEnter
{
    [Subject("State => Default_in_Payment => OnEnter")] // AutoGenerated
    internal class when_default_in_payment : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Default_in_Payment(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}