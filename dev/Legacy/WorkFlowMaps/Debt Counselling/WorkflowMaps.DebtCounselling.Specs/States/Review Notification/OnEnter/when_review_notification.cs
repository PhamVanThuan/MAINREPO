using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Review_Notification.OnEnter
{
    [Subject("State => Review_Notification => OnEnter")] // AutoGenerated
    internal class when_review_notification : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Review_Notification(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}