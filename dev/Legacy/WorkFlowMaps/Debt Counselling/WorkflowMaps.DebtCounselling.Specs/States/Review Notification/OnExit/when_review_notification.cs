using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Review_Notification.OnExit
{
    [Subject("State => Review_Notification => OnExit")] // AutoGenerated
    internal class when_review_notification : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Review_Notification(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}