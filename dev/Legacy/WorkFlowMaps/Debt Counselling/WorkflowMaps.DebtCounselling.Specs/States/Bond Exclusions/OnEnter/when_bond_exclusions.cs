using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Bond_Exclusions.OnEnter
{
    [Subject("State => Bond_Exclusions => OnEnter")] // AutoGenerated
    internal class when_bond_exclusions : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Bond_Exclusions(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}