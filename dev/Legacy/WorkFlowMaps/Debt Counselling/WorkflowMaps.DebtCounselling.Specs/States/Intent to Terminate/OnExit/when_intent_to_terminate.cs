using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Intent_to_Terminate.OnExit
{
    [Subject("State => Intent_to_Terminate => OnExit")] // AutoGenerated
    internal class when_intent_to_terminate : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Intent_to_Terminate(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}