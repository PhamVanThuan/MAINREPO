using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Archive_60days.OnReturn
{
    [Subject("State => Archive_60days => OnReturn")] // AutoGenerated
    internal class when_archive_60days : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnReturn_Archive_60days(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}