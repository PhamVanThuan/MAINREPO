using Machine.Specifications;

namespace WorkflowMaps.DebtCounselling.Specs.States.Archive_60days.OnExit
{
    [Subject("State => Archive_60days => OnExit")] // AutoGenerated
    internal class when_archive_60days : WorkflowSpecDebtCounselling
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Archive_60days(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}