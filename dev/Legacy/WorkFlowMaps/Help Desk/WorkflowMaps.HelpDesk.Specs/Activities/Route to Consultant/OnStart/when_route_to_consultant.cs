using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Route_to_Consultant.OnStart
{
    [Subject("Activity => Route_to_Consultant => OnStart")] // AutoGenerated
    internal class when_route_to_consultant : WorkflowSpecHelpDesk
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Route_to_Consultant(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}