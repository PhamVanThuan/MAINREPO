using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Route_to_Consultant.OnGetActivityMessage
{
    [Subject("Activity => Route_to_Consultant => OnGetActivityMessage")] // AutoGenerated
    internal class when_route_to_consultant : WorkflowSpecHelpDesk
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Route_to_Consultant(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}