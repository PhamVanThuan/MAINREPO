using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Complete_Request.OnGetActivityMessage
{
    [Subject("Activity => Complete_Request => OnGetActivityMessage")] // AutoGenerated
    internal class when_complete_request : WorkflowSpecHelpDesk
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Complete_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}