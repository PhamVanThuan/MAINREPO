using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Complete_Request.OnStart
{
    [Subject("Activity => Complete_Request => OnStart")] // AutoGenerated
    internal class when_complete_request : WorkflowSpecHelpDesk
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Complete_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}