using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.AutoArchive.OnComplete
{
    [Subject("Activity => AutoArchive => OnComplete")] // AutoGenerated
    internal class when_autoarchive : WorkflowSpecHelpDesk
    {
        private static bool result;
        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_AutoArchive(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}