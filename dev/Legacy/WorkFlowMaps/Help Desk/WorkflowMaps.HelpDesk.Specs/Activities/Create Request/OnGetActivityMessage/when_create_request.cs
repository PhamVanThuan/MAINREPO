using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Create_Request.OnGetActivityMessage
{
    [Subject("Activity => Create_Request => OnGetActivityMessage")] // AutoGenerated
    internal class when_create_request : WorkflowSpecHelpDesk
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Create_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}