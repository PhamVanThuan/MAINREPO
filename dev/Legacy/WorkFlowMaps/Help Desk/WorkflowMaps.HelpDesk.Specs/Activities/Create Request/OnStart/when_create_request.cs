using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Create_Request.OnStart
{
    [Subject("Activity => Create_Request => OnStart")] // AutoGenerated
    internal class when_create_request : WorkflowSpecHelpDesk
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Create_Request(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}