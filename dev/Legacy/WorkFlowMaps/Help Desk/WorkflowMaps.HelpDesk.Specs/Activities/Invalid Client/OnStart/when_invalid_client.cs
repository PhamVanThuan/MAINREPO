using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Invalid_Client.OnStart
{
    [Subject("Activity => Invalid_Client => OnStart")] // AutoGenerated
    internal class when_invalid_client : WorkflowSpecHelpDesk
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Invalid_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}