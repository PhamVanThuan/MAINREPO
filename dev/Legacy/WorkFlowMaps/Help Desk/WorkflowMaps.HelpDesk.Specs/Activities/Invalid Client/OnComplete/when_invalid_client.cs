using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Invalid_Client.OnComplete
{
    [Subject("Activity => Invalid_Client => OnComplete")] // AutoGenerated
    internal class when_invalid_client : WorkflowSpecHelpDesk
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Invalid_Client(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}