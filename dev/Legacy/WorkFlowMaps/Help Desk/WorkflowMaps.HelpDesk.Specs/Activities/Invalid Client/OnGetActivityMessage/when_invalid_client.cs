using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Invalid_Client.OnGetActivityMessage
{
    [Subject("Activity => Invalid_Client => OnGetActivityMessage")] // AutoGenerated
    internal class when_invalid_client : WorkflowSpecHelpDesk
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Invalid_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}