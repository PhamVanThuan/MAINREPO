using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.Activities.Proceed.OnGetActivityMessage
{
    [Subject("Activity => Proceed => OnGetActivityMessage")] // AutoGenerated
    internal class when_proceed : WorkflowSpecHelpDesk
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Proceed(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}