using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.States.Confirm_Client.OnEnter
{
    [Subject("State => Confirm_Client => OnEnter")] // AutoGenerated
    internal class when_confirm_client : WorkflowSpecHelpDesk
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Confirm_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}