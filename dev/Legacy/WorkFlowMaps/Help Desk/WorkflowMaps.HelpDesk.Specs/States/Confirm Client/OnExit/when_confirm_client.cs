using Machine.Specifications;

namespace WorkflowMaps.HelpDesk.Specs.States.Confirm_Client.OnExit
{
    [Subject("State => Confirm_Client => OnExit")] // AutoGenerated
    internal class when_confirm_client : WorkflowSpecHelpDesk
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Confirm_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}