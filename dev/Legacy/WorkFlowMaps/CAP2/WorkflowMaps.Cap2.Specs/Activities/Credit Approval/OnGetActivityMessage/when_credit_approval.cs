using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Credit_Approval.OnGetActivityMessage
{
    [Subject("Activity => Credit_Approval => OnGetActivityMessage")] // AutoGenerated
    internal class when_credit_approval : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Credit_Approval(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}