using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.LA_Sent.OnComplete
{
    [Subject("Activity => LA_Sent => OnComplete")] // AutoGenerated
    internal class when_la_sent : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_LA_Sent(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}