using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.LA_Sent.OnGetStageTransition
{
    [Subject("Activity => LA_Sent => OnGetStageTransition")] // AutoGenerated
    internal class when_la_sent : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_LA_Sent(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}