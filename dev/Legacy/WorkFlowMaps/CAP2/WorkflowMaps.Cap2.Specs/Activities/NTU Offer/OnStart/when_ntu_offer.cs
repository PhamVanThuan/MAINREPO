using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.NTU_Offer.OnStart
{
    [Subject("Activity => NTU_Offer => OnStart")] // AutoGenerated
    internal class when_ntu_offer : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_NTU_Offer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}