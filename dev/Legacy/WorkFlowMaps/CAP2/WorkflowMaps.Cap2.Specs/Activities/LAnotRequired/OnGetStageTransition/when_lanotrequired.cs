using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.LAnotRequired.OnGetStageTransition
{
    [Subject("Activity => LAnotRequired => OnGetStageTransition")] // AutoGenerated
    internal class when_lanotrequired : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_LAnotRequired(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}