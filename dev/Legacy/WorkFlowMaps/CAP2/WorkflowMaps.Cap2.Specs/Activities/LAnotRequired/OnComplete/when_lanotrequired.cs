using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.LAnotRequired.OnComplete
{
    [Subject("Activity => LAnotRequired => OnComplete")] // AutoGenerated
    internal class when_lanotrequired : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_LAnotRequired(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}