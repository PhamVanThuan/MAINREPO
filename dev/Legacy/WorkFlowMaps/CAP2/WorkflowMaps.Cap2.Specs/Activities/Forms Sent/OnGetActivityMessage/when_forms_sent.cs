using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Forms_Sent.OnGetActivityMessage
{
    [Subject("Activity => Forms_Sent => OnGetActivityMessage")] // AutoGenerated
    internal class when_forms_sent : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Forms_Sent(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}