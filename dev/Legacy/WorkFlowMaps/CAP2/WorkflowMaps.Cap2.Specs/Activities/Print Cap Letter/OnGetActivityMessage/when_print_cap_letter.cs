using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Print_Cap_Letter.OnGetActivityMessage
{
    [Subject("Activity => Print_Cap_Letter => OnGetActivityMessage")] // AutoGenerated
    internal class when_print_cap_letter : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Print_Cap_Letter(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}