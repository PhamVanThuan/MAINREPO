using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Print_Cap_Letter.OnStart
{
    [Subject("Activity => Print_Cap_Letter => OnStart")] // AutoGenerated
    internal class when_print_cap_letter : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Print_Cap_Letter(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}