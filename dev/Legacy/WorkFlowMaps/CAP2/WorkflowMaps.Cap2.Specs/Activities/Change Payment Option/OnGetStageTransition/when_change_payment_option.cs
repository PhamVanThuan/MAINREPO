using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Change_Payment_Option.OnGetStageTransition
{
    [Subject("Activity => Change_Payment_Option => OnGetStageTransition")] // AutoGenerated
    internal class when_change_payment_option : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Change_Payment_Option(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}