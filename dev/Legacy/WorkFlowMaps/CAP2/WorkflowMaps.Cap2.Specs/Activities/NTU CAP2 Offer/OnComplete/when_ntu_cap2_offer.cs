using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.NTU_CAP2_Offer.OnComplete
{
    [Subject("Activity => NTU_CAP2_Offer => OnComplete")] // AutoGenerated
    internal class when_ntu_cap2_offer : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_NTU_CAP2_Offer(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}