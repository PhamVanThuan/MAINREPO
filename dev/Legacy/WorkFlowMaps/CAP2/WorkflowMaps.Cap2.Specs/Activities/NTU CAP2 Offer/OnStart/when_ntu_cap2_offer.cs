using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.NTU_CAP2_Offer.OnStart
{
    [Subject("Activity => NTU_CAP2_Offer => OnStart")] // AutoGenerated
    internal class when_ntu_cap2_offer : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_NTU_CAP2_Offer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}