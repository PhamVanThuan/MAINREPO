using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Offer_Expired.OnStart
{
    [Subject("Activity => Offer_Expired => OnStart")] // AutoGenerated
    internal class when_offer_expired : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Offer_Expired(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}