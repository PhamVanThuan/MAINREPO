using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Offer_Expired.OnGetStageTransition
{
    [Subject("Activity => Offer_Expired => OnGetStageTransition")] // AutoGenerated
    internal class when_offer_expired : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Offer_Expired(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}