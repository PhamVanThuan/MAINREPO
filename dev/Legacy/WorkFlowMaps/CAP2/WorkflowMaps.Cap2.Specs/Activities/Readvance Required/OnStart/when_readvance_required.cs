using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Readvance_Required.OnStart
{
    [Subject("Activity => Readvance_Required => OnStart")] // AutoGenerated
    internal class when_readvance_required : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Readvance_Required(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}