using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Send_to_Credit.OnComplete
{
    [Subject("Activity => Send_to_Credit => OnComplete")] // AutoGenerated
    internal class when_send_to_credit : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Send_to_Credit(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}