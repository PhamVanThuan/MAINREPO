using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Wait_for_Callback.OnGetStageTransition
{
    [Subject("Activity => Wait_for_Callback => OnGetStageTransition")] // AutoGenerated
    internal class when_wait_for_callback : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Wait_for_Callback(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}