using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Completed_Expired.OnComplete
{
    [Subject("Activity => Completed_Expired => OnComplete")] // AutoGenerated
    internal class when_completed_expired : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Completed_Expired(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}