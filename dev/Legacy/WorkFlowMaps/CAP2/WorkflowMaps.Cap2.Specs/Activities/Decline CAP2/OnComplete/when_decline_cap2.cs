using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Decline_CAP2.OnComplete
{
    [Subject("Activity => Decline_CAP2 => OnComplete")] // AutoGenerated
    internal class when_decline_cap2 : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Decline_CAP2(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}