using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Decline_CAP2.OnStart
{
    [Subject("Activity => Decline_CAP2 => OnStart")] // AutoGenerated
    internal class when_decline_cap2 : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Decline_CAP2(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}