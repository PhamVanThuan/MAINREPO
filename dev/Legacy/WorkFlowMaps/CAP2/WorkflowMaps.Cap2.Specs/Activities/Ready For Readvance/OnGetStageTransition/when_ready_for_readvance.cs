using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Ready_For_Readvance.OnGetStageTransition
{
    [Subject("Activity => Ready_For_Readvance => OnGetStageTransition")] // AutoGenerated
    internal class when_ready_for_readvance : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Ready_For_Readvance(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}