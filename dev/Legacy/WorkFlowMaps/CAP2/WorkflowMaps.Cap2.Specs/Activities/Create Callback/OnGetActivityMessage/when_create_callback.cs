using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Create_Callback.OnGetActivityMessage
{
    [Subject("Activity => Create_Callback => OnGetActivityMessage")] // AutoGenerated
    internal class when_create_callback : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Create_Callback(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}