using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Create_Callback.OnComplete
{
    [Subject("Activity => Create_Callback => OnComplete")] // AutoGenerated
    internal class when_create_callback : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            string message = string.Empty;
            result = workflow.OnCompleteActivity_Create_Callback(instanceData, workflowData, paramsData, messages, ref message);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}