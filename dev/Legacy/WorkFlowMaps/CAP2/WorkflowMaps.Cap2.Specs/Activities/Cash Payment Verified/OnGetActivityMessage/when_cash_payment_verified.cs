using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Cash_Payment_Verified.OnGetActivityMessage
{
    [Subject("Activity => Cash_Payment_Verified => OnGetActivityMessage")] // AutoGenerated
    internal class when_cash_payment_verified : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Cash_Payment_Verified(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}