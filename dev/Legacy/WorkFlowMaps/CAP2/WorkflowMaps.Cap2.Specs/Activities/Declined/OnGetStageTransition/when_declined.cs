using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Declined.OnGetStageTransition
{
    [Subject("Activity => Declined => OnGetStageTransition")] // AutoGenerated
    internal class when_declined : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetStageTransition_Declined(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}