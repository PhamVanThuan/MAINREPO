using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Declined.OnStart
{
    [Subject("Activity => Declined => OnStart")] // AutoGenerated
    internal class when_declined : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnStartActivity_Declined(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}