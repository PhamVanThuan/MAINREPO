using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Further_Advance_Decision.OnGetActivityMessage
{
    [Subject("Activity => Further_Advance_Decision => OnGetActivityMessage")] // AutoGenerated
    internal class when_further_advance_decision : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Further_Advance_Decision(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}