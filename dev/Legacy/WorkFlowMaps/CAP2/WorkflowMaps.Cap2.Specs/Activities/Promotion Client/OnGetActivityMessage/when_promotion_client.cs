using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.Activities.Promotion_Client.OnGetActivityMessage
{
    [Subject("Activity => Promotion_Client => OnGetActivityMessage")] // AutoGenerated
    internal class when_promotion_client : WorkflowSpecCap2
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Promotion_Client(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}