using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.States.CAP2_Expired.OnReturn
{
    [Subject("State => CAP2_Expired => OnReturn")] // AutoGenerated
    internal class when_cap2_expired : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnReturn_CAP2_Expired(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}