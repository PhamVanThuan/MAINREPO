using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.States.Ready_to_Callback.OnExit
{
    [Subject("State => Ready_to_Callback => OnExit")] // AutoGenerated
    internal class when_ready_to_callback : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Ready_to_Callback(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}