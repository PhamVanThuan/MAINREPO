using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.States.Forward_to_last_State.OnExit
{
    [Subject("State => Forward_to_last_State => OnExit")] // AutoGenerated
    internal class when_forward_to_last_state : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Forward_to_last_State(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}