using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.States.Common_Change_Payment_Option.OnEnter
{
    [Subject("State => Common_Change_Payment_Option => OnEnter")] // AutoGenerated
    internal class when_common_change_payment_option : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_Common_Change_Payment_Option(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}