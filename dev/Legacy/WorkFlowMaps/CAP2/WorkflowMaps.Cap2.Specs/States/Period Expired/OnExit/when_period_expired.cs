using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.States.Period_Expired.OnExit
{
    [Subject("State => Period_Expired => OnExit")] // AutoGenerated
    internal class when_period_expired : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Period_Expired(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}