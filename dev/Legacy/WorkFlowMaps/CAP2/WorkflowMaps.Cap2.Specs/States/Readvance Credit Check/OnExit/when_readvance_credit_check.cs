using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.States.Readvance_Credit_Check.OnExit
{
    [Subject("State => Readvance_Credit_Check => OnExit")] // AutoGenerated
    internal class when_readvance_credit_check : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Readvance_Credit_Check(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}