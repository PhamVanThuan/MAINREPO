using Machine.Specifications;

namespace WorkflowMaps.Cap2.Specs.States.NTU_Offer.OnEnter
{
    [Subject("State => NTU_Offer => OnEnter")] // AutoGenerated
    internal class when_ntu_offer : WorkflowSpecCap2
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnEnter_NTU_Offer(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}