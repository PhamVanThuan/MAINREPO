'use strict';
describe('[sahl.halo.app.variables.app]', function () {
    beforeEach(module('sahl.halo.app.variables.app'));
    beforeEach(inject(function ($injector, $q) {
    }));

    describe(' - (Service: app)-', function () {

    });
});