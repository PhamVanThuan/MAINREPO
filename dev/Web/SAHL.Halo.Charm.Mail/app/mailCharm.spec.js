'use strict';
describe('[sahl.halo.charm.mail.app]', function () {
    beforeEach(module('sahl.halo.charm.mail.app'));
    beforeEach(inject(function ($injector, $q) {
    }));

    describe(' - (Service: mailCharm)-', function () {

    });
});