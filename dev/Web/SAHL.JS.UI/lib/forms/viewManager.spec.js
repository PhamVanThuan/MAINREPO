'use strict';
describe('[sahl.js.ui.forms]', function () {
    beforeEach(module('sahl.js.ui.forms'));
    beforeEach(inject(function ($injector, $q) {
    }));

    describe(' - (Service: viewManager)-', function () {

    });
});