'use strict';
describe('[sahl.halo.app.organisationStructure.organisationStructureQueryService]', function () {
    beforeEach(module('sahl.halo.app.organisationStructure.organisationStructureQueryService'));
    beforeEach(inject(function ($injector, $q) {
    }));

    describe(' - (Service: organisationStructureQueryService)-', function () {

    });
});