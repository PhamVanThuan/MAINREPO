'use strict';
describe('[sahl.halo.app.organisationStructure.app]', function () {
    beforeEach(module('sahl.halo.app.organisationStructure.app'));
    beforeEach(inject(function ($injector, $q) {
    }));

    describe(' - (Service: app)-', function () {

    });
});