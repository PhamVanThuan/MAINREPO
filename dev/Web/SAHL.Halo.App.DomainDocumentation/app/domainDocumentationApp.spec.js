'use strict';
describe('[sAHL.halo.app.domainDocumentation.app]', function () {
    beforeEach(module('sahl.halo.app.domainDocumentation.app'));
    beforeEach(inject(function ($injector, $q) {
    }));

    describe(' - (Service: domainDocumentationApp)-', function () {

    });
});