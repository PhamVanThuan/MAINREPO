'use strict';
describe('[sahl.js.core.primitives]', function () {
    beforeEach(module('sahl.js.core.primitives'));
    beforeEach(inject(function ($injector, $q) {
    }));

    describe(' - (Service: combGuid)-', function () {

    });
});