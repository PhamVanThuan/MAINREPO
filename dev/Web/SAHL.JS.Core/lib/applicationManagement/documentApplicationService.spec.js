'use strict';
describe('[sahl.js.core.applicationManagement]', function () {
    beforeEach(module('sahl.js.core.applicationManagement'));
    beforeEach(inject(function ($injector, $q) {
    }));

    describe(' - (Service: documentApplicationService)-', function () {

    });
});