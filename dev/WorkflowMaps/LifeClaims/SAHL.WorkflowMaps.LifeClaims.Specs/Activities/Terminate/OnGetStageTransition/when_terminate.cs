using Machine.Specifications;

namespace WorkflowMaps.DisabilityClaim.Specs.Activities.Terminate.OnGetStageTransition
{
    [Subject("Activity => Terminate => OnGetStageTransition")] // AutoGenerated
    internal class when_terminate : WorkflowSpecDisabilityClaim
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_Terminate(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}