using Machine.Specifications;
using SAHL.WorkflowMaps.LifeClaims.Specs;

namespace WorkflowMaps.DisabilityClaim.Specs.Activities.Create_Disability_Claim.OnGetActivityMessage
{
    [Subject("Activity => Create_Disability_Claim => OnGetActivityMessage")] // AutoGenerated
    internal class when_create_disability_claim : WorkflowSpecDisabilityClaim
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetActivityMessage_Create_Disability_Claim(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}