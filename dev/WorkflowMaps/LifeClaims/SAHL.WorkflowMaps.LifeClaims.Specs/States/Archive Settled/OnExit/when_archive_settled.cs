using Machine.Specifications;

namespace WorkflowMaps.DisabilityClaim.Specs.States.Archive_Settled.OnExit
{
    [Subject("State => Archive_Settled => OnExit")] // AutoGenerated
    internal class when_archive_settled : WorkflowSpecDisabilityClaim
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnExit_Archive_Settled(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}