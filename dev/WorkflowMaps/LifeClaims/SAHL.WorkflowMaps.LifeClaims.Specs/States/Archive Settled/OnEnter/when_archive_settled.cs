using Machine.Specifications;

namespace WorkflowMaps.DisabilityClaim.Specs.States.Archive_Settled.OnEnter
{
    [Subject("State => Archive_Settled => OnEnter")] // AutoGenerated
    internal class when_archive_settled : WorkflowSpecDisabilityClaim
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnEnter_Archive_Settled(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}