using Machine.Specifications;

namespace WorkflowMaps.DisabilityClaim.Specs.States.Archive_Terminated.OnExit
{
    [Subject("State => Archive_Terminated => OnExit")] // AutoGenerated
    internal class when_archive_terminated : WorkflowSpecDisabilityClaim
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnExit_Archive_Terminated(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}