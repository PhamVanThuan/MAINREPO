using Machine.Specifications;

namespace SAHL.WorkflowMaps.ThirdPartyInvoices.Specs.States.Invoice_Captured.OnExit
{
    [Subject("State => Invoice_Captured => OnExit")] // AutoGenerated
    internal class when_invoice_captured : WorkflowSpecThirdPartyInvoices
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnExit_Invoice_Captured(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}