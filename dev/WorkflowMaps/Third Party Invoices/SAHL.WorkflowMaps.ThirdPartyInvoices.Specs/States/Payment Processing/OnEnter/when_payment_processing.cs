using Machine.Specifications;
using SAHL.WorkflowMaps.ThirdPartyInvoices.Specs;

namespace WorkflowMaps.ThirdPartyInvoices.Specs.States.Payment_Processing.OnEnter
{
    [Subject("State => Payment_Processing => OnEnter")] // AutoGenerated
    internal class when_payment_processing : WorkflowSpecThirdPartyInvoices
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnEnter_Payment_Processing(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}