using Machine.Specifications;
using SAHL.WorkflowMaps.ThirdPartyInvoices.Specs;

namespace WorkflowMaps.ThirdPartyInvoices.Specs.States.Payment_Processing.OnExit
{
    [Subject("State => Payment_Processing => OnExit")] // AutoGenerated
    internal class when_payment_processing : WorkflowSpecThirdPartyInvoices
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnExit_Payment_Processing(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}