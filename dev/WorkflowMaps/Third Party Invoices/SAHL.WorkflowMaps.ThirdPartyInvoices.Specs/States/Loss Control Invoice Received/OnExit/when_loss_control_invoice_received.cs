using Machine.Specifications;

namespace SAHL.WorkflowMaps.ThirdPartyInvoices.Specs.States.Loss_Control_Invoice_Received.OnExit
{
    [Subject("State => Loss_Control_Invoice_Received => OnExit")] // AutoGenerated
    internal class when_loss_control_invoice_received : WorkflowSpecThirdPartyInvoices
    {
        private static bool result;

        private Establish context = () =>
        {
            result = false;
        };

        private Because of = () =>
        {
            result = workflow.OnExit_Loss_Control_Invoice_Received(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}