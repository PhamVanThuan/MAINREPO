using Machine.Specifications;

namespace WorkflowMaps.ThirdPartyInvoices.Specs.Activities.Create_Case.OnGetActivityMessage
{
    [Subject("Activity => Create_Case => OnGetActivityMessage")] // AutoGenerated
    internal class when_create_case : WorkflowSpecThirdPartyInvoices
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetActivityMessage_Create_Case(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}