using Machine.Specifications;

namespace WorkflowMaps.ThirdPartyInvoices.Specs.Activities.Create_Case.OnGetStageTransition
{
    [Subject("Activity => Create_Case => OnGetStageTransition")] // AutoGenerated
    internal class when_create_case : WorkflowSpecThirdPartyInvoices
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetStageTransition_Create_Case(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}