using Machine.Specifications;

namespace WorkflowMaps.ThirdPartyInvoices.Specs.Activities.Create_Case.OnStart
{
    [Subject("Activity => Create_Case => OnStart")] // AutoGenerated
    internal class when_create_case : WorkflowSpecThirdPartyInvoices
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_Create_Case(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}