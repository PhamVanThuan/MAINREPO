using Machine.Specifications;

namespace WorkflowMaps.ThirdPartyInvoices.Specs.Activities.isLossControlInvoice.OnStart
{
    [Subject("Activity => isLossControlInvoice => OnStart")] // AutoGenerated
    internal class when_islosscontrolinvoice : WorkflowSpecThirdPartyInvoices
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_isLossControlInvoice(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}