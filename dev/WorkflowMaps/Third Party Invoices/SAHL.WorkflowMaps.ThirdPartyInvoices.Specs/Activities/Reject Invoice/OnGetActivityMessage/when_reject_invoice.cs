using Machine.Specifications;

namespace SAHL.WorkflowMaps.ThirdPartyInvoices.Specs.Activities.Reject_Invoice.OnGetActivityMessage
{
    [Subject("Activity => Reject_Invoice => OnGetActivityMessage")] // AutoGenerated
    internal class when_reject_invoice : WorkflowSpecThirdPartyInvoices
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Reject_Invoice(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}