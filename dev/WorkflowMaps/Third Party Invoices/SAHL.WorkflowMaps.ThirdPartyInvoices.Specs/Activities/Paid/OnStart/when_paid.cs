using Machine.Specifications;
using SAHL.WorkflowMaps.ThirdPartyInvoices.Specs;

namespace WorkflowMaps.ThirdPartyInvoices.Specs.Activities.Paid.OnStart
{
    [Subject("Activity => Paid => OnStart")] // AutoGenerated
    internal class when_paid : WorkflowSpecThirdPartyInvoices
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_Paid(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}