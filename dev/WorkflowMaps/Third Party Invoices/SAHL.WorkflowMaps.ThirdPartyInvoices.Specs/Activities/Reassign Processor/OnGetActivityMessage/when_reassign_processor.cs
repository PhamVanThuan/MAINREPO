using Machine.Specifications;

namespace SAHL.WorkflowMaps.ThirdPartyInvoices.Specs.Activities.Reassign_Processor.OnGetActivityMessage
{
    [Subject("Activity => Reassign_Processor => OnGetActivityMessage")] // AutoGenerated
    internal class when_reassign_processor : WorkflowSpecThirdPartyInvoices
    {
        private static string result;

        private Establish context = () =>
        {
            result = "abcd";
        };

        private Because of = () =>
        {
            result = workflow.GetActivityMessage_Reassign_Processor(instanceData, workflowData, paramsData, messages);
        };

        private It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}