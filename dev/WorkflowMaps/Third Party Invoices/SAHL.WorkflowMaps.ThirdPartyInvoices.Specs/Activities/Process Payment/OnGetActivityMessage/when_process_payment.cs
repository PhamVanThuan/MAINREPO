using Machine.Specifications;
using SAHL.WorkflowMaps.ThirdPartyInvoices.Specs;

namespace WorkflowMaps.ThirdPartyInvoices.Specs.Activities.Process_Payment.OnGetActivityMessage
{
    [Subject("Activity => Process_Payment => OnGetActivityMessage")] // AutoGenerated
    internal class when_process_payment : WorkflowSpecThirdPartyInvoices
    {
        static string result;
        Establish context = () =>
        {
            result = "abcd";
        };

        Because of = () =>
        {
            result = workflow.GetActivityMessage_Process_Payment(instanceData, workflowData, paramsData, messages);
        };

        It should_return_an_empty_string = () =>
        {
            result.ShouldBeTheSameAs(string.Empty);
        };
    }
}