using Machine.Specifications;
using SAHL.WorkflowMaps.ThirdPartyInvoices.Specs;

namespace WorkflowMaps.ThirdPartyInvoices.Specs.Activities.Process_Payment.OnStart
{
    [Subject("Activity => Process_Payment => OnStart")] // AutoGenerated
    internal class when_process_payment : WorkflowSpecThirdPartyInvoices
    {
        static bool result;
        Establish context = () =>
        {
            result = false;
        };
		
        Because of = () =>
        {
            result = workflow.OnStartActivity_Process_Payment(instanceData, workflowData, paramsData, messages);
        };

        It should_return_true = () =>
        {
            result.ShouldBeTrue();
        };
    }
}